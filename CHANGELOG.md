<div dir="rtl">

# تاریخچهٔ تغییرات (Changelog)

این فایل مهم‌ترین تغییرات پروژه را به‌ترتیب نسخه فهرست می‌کند.
تاریخ‌ها شمسی و توضیحات فارسی هستند (اصطلاحات فنی به انگلیسی).

</div>

---

## نسخهٔ ۲.۲.۸ — رفع خطای آمار و تعمیرات پس از ری‌استارت + جداسازی هندلر نمایندگی‌ها

- رفع TypeError گزارش‌شدهٔ `_server_resource_line`: دکوراتور ادمین روی هندلر اصلی قرار گرفت؛ نمونه‌برداری منابع به ترد منتقل شد.
- اصلاح ترتیب راه‌اندازی اصلی/نمایندگی: ابتدا initialize/pickle، سپس هویت ربات و پرچم تعمیرات از دیتابیس؛ جلوگیری از بازنویسی با وضعیت قدیمی.
- کنترل تعمیرات برای همهٔ Updateها؛ شکست اعلان یا خواندن اولیهٔ وضعیت، مجوز عبور کاربر نیست.
- مرجع واحد ذخیرهٔ تعمیرات (`bot_id=1`)، انتشار تغییر به اپ‌های فعال/در حال شروع و ممنوعیت نمایش موفقیت در خطای ذخیره.
- بررسی مجدد پیش از کسر وجه و شروع رزروها؛ قفل مشترک پذیرش نهایی با تغییر وضعیت تعمیرات.
- سوپرادمینِ لغودسترسی‌شده دیگر با نقش قدیمی از تعمیرات عبور نمی‌کند.
- رجیستری ConversationHandler بر اساس Application جدا شد؛ ثبت نمایندگی دیگر منوهای ربات اصلی را مختل نمی‌کند.
- اصلاح clear_conversations برای /start و اصلاح newlineهای متن پنل تعمیرات.
- ۲۰ تست اجرایی جدید، شامل فایل pickle واقعی و dispatcher واقعی PTB؛ مجموع ۲۵۴ تست موفق با PostgreSQL.

[راهنمای فارسی نصب و تست پذیرش](docs/MAINTENANCE_FIX_2.2.8_FA.md)

---

## نسخهٔ ۲.۲.۷ — لغو و عودت اتمیک + رفع تداخل هندلرها + کاهش سربار CPU

- مبنا: آخرین پوش `7a2fc8e` شاخهٔ `arena/01a0b3b3-tgcallbot`؛ گارد منابع واقعی و اجرای موجی محفوظ.
- رفع علت اصلی دکمهٔ لغو: جداکردن گروه‌های ضداسپم، تعمیرات، لغو و پیش‌روتر PTB؛ تست با dispatcher واقعی.
- تسویهٔ مشترک کاربر/ادمین: وضعیت، کیف پول، تراکنش و رسید ماندگار در یک commit با قفل ردیف.
- جلوگیری از عودت تکراری، بازاجرای سفارش لغوشده و از‌دست‌رفتن افزایش همزمان موجودی.
- برگشت کامل برای انتظار/build بدون شروع سرویس؛ استفاده از پیشرفت واقعی در پیش‌نمایش سفارش حجمی.
- رسید مالی فارسی و ارسال پیام جایگزین وقتی ویرایش پیام قبلی ممکن نیست؛ کنترل مالکیت و نقش ادمین.
- حفظ شناسایی منوهای ثابت پریمیوم پس از ری‌استارت/خاموش‌کردن قابلیت.
- کش محدود و single-flight فقط برای اعتبارسنجی فرمان سکوت FFmpeg؛ محدودکردن تردهای filter/encoder بدون حذف استریم و بازیابی.
- افزودن جدول `order_settlements` بدون حذف دادهٔ قبلی؛ پشتیبانی در بکاپ/بازیابی JSON.
- اصلاح ایزولاسیون تست‌های قدیمی که singletonهای صوتی را با stub جایگزین و تست‌های بعدی را خراب می‌کردند.
- ۲۳۴ تست موفق در محیط توسعه، شامل PostgreSQL واقعی، rollback در commit، ۲۰ لغو همزمان و استریم واقعی FFmpeg.
- بنچمارک: ۵۰ اجرای راهنمای FFmpeg به ۱ رسید؛ حدود ۹۸٪ کاهش CPU **این بخش**، نه ادعای کاهش CPU کل ربات.

راهنمای فارسی، اعداد اندازه‌گیری، محدودیت‌ها و دستور استقرار:
[CPU_AND_CANCEL_2.2.7_FA.md](docs/CPU_AND_CANCEL_2.2.7_FA.md)

---

## نسخهٔ ۲.۲.۶ — اصلاحِ گارد منابع: اندازه‌گیری از «خودِ سرور»، بدون هیچ عددِ ساختگی

<div dir="rtl">

### چه اشتباهی بود؟ (اصلاح با توضیحِ شما)
در ۲.۲.۵ دو اشتباه اساسی کرده بودم که با توضیح شما اصلاح شد:

1. **اعدادِ ساختگی**: برای مدل یک پیش‌فرض گذاشته بودم («۴۵ مگابایت رم به‌ازای هر
   اکانت») که شما هرگز نخواسته بودید. از این نسخه هیچ عددی اختراع نمی‌شود؛
   هزینهٔ هر اکانت فقط از **اندازه‌گیریِ واقعی** یاد گرفته می‌شود
   ((مصرفِ فعلی − مصرفِ مرجع) تقسیم بر اختلافِ تعداد اکانت‌ها).
2. **اندازه‌گیریِ اشتباه**: منابع را با سقفِ کانتینر (۱.۵ گیگابایت / ۲ هسته)
   می‌سنجید در حالی که منظور شما **سرور** بود (۱۶ گیگ رم، ۸ هسته). حوزهٔ
   پیش‌فرض حالا `server` است — همان اعدادی که در htop می‌بینید.

### قانونِ دقیقی که الان پیاده شده
لحظهٔ ثبت سفارش (آنی یا زمان‌بندی‌شده):

1. اول چک می‌شود چه سفارش‌هایی از قبل فعال‌اند و در بازهٔ درخواستی ادامه دارند
   (از `start` تا `start+مدت`).
2. مصرفِ منابعِ سرور در آن زمان اندازه‌گیری می‌شود:
   - **سفارش آنی** → اندازه‌گیریِ واقعیِ همین لحظهٔ سرور (CPU/رم/بار) که بارِ
     همهٔ سفارش‌های در حال اجرا را هم در خود دارد.
   - **سفارش زمان‌بندی‌شده** → اگر هزینهٔ هر اکانت از اندازه‌گیری‌های واقعی
     به‌دست آمده، با آن پیش‌بینی می‌شود؛ وگرنه اصلاً حدس زده نمی‌شود و فقط یک
     استنتاجِ مستقیم اعمال می‌گردد: اگر سرور همین الآن بالای آستانه است و
     سفارش‌هایی که این بار را ساخته‌اند در بازهٔ درخواستی هم هستند، پس در آن
     زمان هم درگیر خواهد بود → رد.
3. **اگر ۸۵٪ منابع درگیر باشد → سفارش پذیرفته نمی‌شود**، و به‌جای «بعداً امتحان
   کنید»، دقیق‌ترین زمانی که سرور آزاد می‌شود اعلام می‌گردد (مثال: درخواستِ
   ۱۲:۱۰ در حالی که سرور تا ۱۳:۳۵ درگیر است، رد می‌شود و ساعتِ بعد از ۱۳:۳۵
   پیشنهاد می‌گردد).

### تغییراتِ فنی
- `services/system_resources.py`: حوزهٔ `server` (پیش‌فرض) اضافه شد که
  اشتغالِ کل سرور را از `/proc/meminfo` و `/proc/stat` می‌خواند؛ حوزهٔ
  `container` برای سقفِ کانتینر محفوظ ماند.
- `ResourceCost` پیش‌فرضش صفر شد (یعنی «اندازه‌گیری نشده») و `is_measured()`
  اضافه شد؛ آستانهٔ پیش‌فرض برای هر دو بُعد ۸۵ درصد است.
- `ResourceCalibrator` بازنویسی شد: کمترین مصرفِ دیده‌شده نقطهٔ مرجع می‌شود و
  هزینهٔ هر اکانت فقط از تفاضلِ اندازه‌گیری‌ها به‌دست می‌آید؛ تا وقتی داده‌ای
  وجود نداشته باشد، `cost()` مقدار `None` برمی‌گرداند (هیچ حدسی).
- تنظیماتِ جدید: `CAPACITY_MAX_RESOURCE_PERCENT=85` (آستانهٔ واحد) و
  `CAPACITY_RESOURCE_SCOPE=server|container`.

### رد کردنِ «سفارشِ بزرگ‌تر از ظرفیت» حذف شد ⛔️➡️🔄
پیامِ «درخواست شما ۵۰ اکانت است، اما کل ظرفیت سالم سرور ۳۵ اکانت است؛ لطفاً
پلن کوچک‌تری انتخاب کنید» **غلط بود** و حذف شد. طبق اصلِ محصولیِ پروژه
(که در خودِ `config.py` هم نوشته شده) تعدادِ کلِ سفارش محدودیتی ندارد و فقط به
اکانت‌های واجد شرایط و ظرفیتِ سرور محدود است؛ اجراکننده از قبل **موج‌بندی**
(`_voice_batched_fill`) داشت: هر موج تا سقفِ اکانت‌های آزاد وارد می‌شود و پس از
پایان، موجِ بعدی از همان پول جایگزین می‌گردد. بنابراین:

- بُعدِ «اکانت» تنها زمانی رد می‌کند که در **کل بازهٔ درخواستی هیچ اکانتِ
  آزادی وجود نداشته باشد** (پول کاملاً اشغال)؛
- سفارشِ بزرگ‌تر با موج‌بندی پذیرفته می‌شود و رأی (verdict) تعدادِ موج‌ها و
  ظرفیتِ همزمان را گزارش می‌دهد: «در ۲ موج × ۳۱ اکانت اجرا می‌شود»؛
- محافظِ اصلی در برابرِ ازدحام همان **بُعدِ منابع (۸۵٪ بر اساس اندازه‌گیری
  واقعی)** است، نه یک مقایسهٔ حسابیِ تعداد اکانت‌ها؛
- پیشنهادِ ساعت حالا برای سفارش‌های بزرگ هم داده می‌شود (قبلاً یک نگهبانِ قدیمی
  مانع می‌شد).

### تست‌ها
`tests/test_system_resources.py` به‌روز شد: کالیبراسیون فقط از دادهٔ واقعی،
ردِّ سفارشِ آنی روی سرورِ ۹۱٪ درگیر، عدمِ حدس برای بازهٔ آینده، و استنتاجِ
«سرور الآن شلوغ است و سفارش‌ها در این بازه هم هستند» (سناریوی ۱۲:۱۰ در حالی که
تا ۱۳:۳۵ درگیر است). مجموع: **۱۳۷ تست**.

</div>

---

## نسخهٔ ۲.۲.۵ — 🖥 گارد ظرفیتِ دوبُعدی: اکانت‌ها **و** منابع واقعی سرور (CPU / RAM / Load)

<div dir="rtl">

### چرا؟ (مسئلهٔ محصولی)
گارد ظرفیت تا ۲.۲.۴ فقط «تعداد اکانت سالم» را به‌عنوان منبعِ اشتراکی می‌دید؛
در حالی که فشار واقعی روی **پردازنده، حافظه و بار سیستم** است: هر اکانتِ حاضر در
ویس‌کال یک کلاینت MTProto + جریان ffmpeg است. اگر سرور در بازهٔ اجرای سفارش زیرِ
فشار باشد، پذیرش سفارشِ جدید یعنی افت کیفیت برای همه و در بدترین حالت **لغو
زنجیره‌ای سفارش‌ها**. درخواستِ محصول دقیقاً همین بود: «قبل از تأیید سفارش،
منابع سیستم مثل رم و سی‌پی‌یو را در آن ساعت بسنجد و اگر بار طوری است که همهٔ
سفارش‌ها کنسل می‌شوند، نپذیرد و بگوید در فلان ساعت اقدام کن».

### چه اضافه شد؟
1. **ماژول جدید `services/system_resources.py`** — اندازه‌گیریِ مستقیم و بدون
   وابستگیِ جدید (فقط `/proc` و cgroup v1/v2، **بدون psutil**):
   - خواندنِ رم با **سقفِ کانتینر** (`memory.max` / `memory.current`) و در نبودِ آن
     `MemAvailable` از `/proc/meminfo`؛
   - خواندنِ CPU با `cpu.stat` + `cpu.max` (درصد نسبت به سهمیهٔ واقعیِ کانتینر)
     و در نبودِ آن اختلافِ دو نمونهٔ `/proc/stat`؛
   - `Load Average` از `/proc/loadavg` نرمال‌شده بر تعداد هسته؛
   - همهٔ پارس‌کننده‌ها **توابع خالص** هستند (ورودی = متن فایل) ⇒ کاملاً تست‌پذیر.
2. **پیش‌بینی روی کل بازه، نه لحظهٔ شروع** — مدل خطی:
   `مصرف(t) = خط‌مبنای سیستم + هزینهٔ هر اکانت × تعداد اکانت‌های همزمان(t)`.
   بنابراین داوری روی «اوجِ مصرف در کل بازهٔ start → start+مدت» انجام می‌شود،
   درست همان‌طور که برای اکانت‌ها انجام می‌شد.
3. **کالیبراسیونِ تطبیقی** (`ResourceCalibrator`): هزینهٔ هر اکانت و خط‌مبنای
   سیستم با میانگین متحرک نمایی از نمونه‌های واقعی یاد گرفته می‌شود؛ ادمین
   می‌تواند مقادیر را در `.env` قفل کند.
4. **یکپارچه‌سازی با هستهٔ محاسباتی**: `check_capacity` و `earliest_fit_start`
   حالا پارامترهای `system` / `baseline` / `cost` / `limits` می‌گیرند؛ یعنی
   ساعتِ پیشنهادی فقط زمانی است که **هم ظرفیت اکانت و هم CPU/RAM** جا بدهند.
   مثالِ تست‌شده: با رزروِ سنگین تا ۱۳:۳۵، درخواستِ ۱۲:۱۰ رد می‌شود و پیشنهاد
   بعد از ۱۳:۳۵ است — نه «ده دقیقهٔ دیگر».
5. **نمونه‌برداریِ غیرِ مسدودکننده**: با `asyncio.to_thread` (حدود ۰.۳۵ ثانیه)
   تا حلقهٔ رویدادِ ربات بلاک نشود.
6. **شفافیت برای کاربر و ادمین**: پیامِ ردّ حالا اعداد واقعی را می‌گوید
   (مصرف فعلی + پیش‌بینی در اوج بازه + سقف مجاز + عامل اصلی: CPU/RAM/Load)،
   پیش‌نمایشِ صفحهٔ تأیید یک خط وضعیت منابع نشان می‌دهد و «📉 آمار کلی ربات»
   بخش «🖥 منابع سرور» دارد.

### تنظیمات جدید (`.env`)
```env
CAPACITY_CHECK_SYSTEM_LOAD=true      # فعال‌سازی بُعد منابع
CAPACITY_MAX_CPU_PERCENT=85          # سقفِ پیش‌بینی CPU در اوج بازه
CAPACITY_MAX_MEMORY_PERCENT=88       # سقفِ پیش‌بینی RAM در اوج بازه
CAPACITY_MAX_LOAD_PER_CORE=1.5       # سقفِ Load به‌ازای هر هسته (فقط بازه‌های نزدیک)
CAPACITY_SAMPLE_INTERVAL_SEC=0.35
CAPACITY_CPU_PERCENT_PER_ACCOUNT=0   # ۰ = کالیبراسیون خودکار
CAPACITY_MEMORY_MB_PER_ACCOUNT=0
CAPACITY_BASELINE_CPU_PERCENT=0
CAPACITY_BASELINE_MEMORY_MB=0
```

### رفتارِ ایمن (Fail-open)
هر خطا در اندازه‌گیری (مثلاً محیط غیرلینوکس یا نبودِ `/proc`) با `ok=False`
برمی‌گردد و آن بُعد نادیده گرفته می‌شود؛ **هیچ نقصِ اندازه‌گیری‌ای هرگز جلوی ثبت
سفارش را نمی‌گیرد**. دروازهٔ Load فقط برای بازه‌های نزدیک (۱۵ دقیقه) اعمال
می‌شود، چون بارِ فعلی دربارهٔ سه ساعتِ دیگر اطلاعی نمی‌دهد.

### 🧪 ممیزیِ سراسریِ منوها (در همین نسخه، به درخواستِ شما)
یک ممیزیِ **خودکار و ایستا** نوشته شد (`tests/test_menu_audit.py`) که بدون اجرای
ربات و بدون پکیج telegram بررسی می‌کند هیچ دکمه‌ای در منو تعریف نشود مگر اینکه
هندلری آن را مسیردهی کند. روش: استخراج همهٔ لیبل‌های منو از `constants.py`
(با حلِ ارجاعاتی مثل `BTN_BACK` و `MAIN_MENU = USER_MAIN_MENU`)، استخراج
همهٔ مسیرهای واقعی از `main.py` (از جمله الگوهای ساخته‌شده با اتصالِ چند
رشته، f-string و متغیّرِ فیلتر مثل `FILTER_BACK`) و در نهایت تطبیقِ
زیررشته‌ایِ رایج در هندلرها. نکتهٔ کلیدی: فیلترِ «طردی»
(`~FILTER_NAV_BUTTONS` داخل `STD_TEXT`) مسیر محسوب نمی‌شود — وگرنه دکمه‌های
مرده «تحت‌پوشش» دیده می‌شدند.

نتیجهٔ ممیزی و اصلاحات:
- **۴ دکمهٔ مرده** در `SECURITY_SETTINGS_MENU` پیدا و ثابتِ بلااستفاده حذف شد
  (منوی امنیتیِ واقعی اینلاین است و با کال‌بک‌های `sec_toggle_*` کار می‌کند).
- سه ثابتِ منویِ فراموش‌شدهٔ دیگر (`ADMIN_SETTINGS_MENU`، `USER_MANAGEMENT_MENU`،
  `ORDER_MENU`) که هیچ استفاده‌ای نداشتند حذف شدند — منوهای واقعی در خودِ
  هندلرها ساخته می‌شوند.
- لاگِ دیباگِ موقت (`callback seen: ...`) از نگهبان تعمیرات حذف شد.
- **تسویهٔ عادلانهٔ سفارشِ در صف**: سفارشِ `pending` که هنوز اجرا نشده بود، از
  روی `created_at` شارژ می‌شد (یعنی کاربر برای انتظار در صف پول می‌داد)؛ حالا
  مثل `scheduled` عودتِ کامل می‌گیرد مگر اینکه واقعاً `started_at` داشته باشد.
- **ایمن‌سازیِ چندمستأجری**: `get_orders_history` حالا `bot_id` می‌گیرد و هر سه
  محلِ فراخوانی (لغوِ دسته‌ایِ ادمین، تاریخچهٔ کاربر در پنل ادمین، تاریخچهٔ
  کاربر) آن را پاس می‌دهند تا داده بین نمایندگی‌ها نشت نکند.
- حالت تعمیرات متن، دستور و کال‌بک را می‌بندد و با `ApplicationHandlerStop`
  انتشار را متوقف می‌کند (بررسیِ خودکار در همان تست قفل شده است).

### تست‌ها
`tests/test_system_resources.py` (۳۰ تست آفلاین) + `tests/test_menu_audit.py`
(ممیزیِ خودکارِ منوها) + ۵ تستِ جدید در `test_menu_and_lifecycle_fixes.py`: پارس‌کننده‌ها (نمونه‌های
واقعیِ `/proc/meminfo` و `/proc/stat` و cgroup)، پیش‌بینی، کالیبراسیون، و
یکپارچه‌سازی با گارد ظرفیت (ردِّ پنجره‌ای، پیشنهادِ دقیق، fail-open).
مجموع: **۱۲۹ تست** (۱۰ خطایِ محیطیِ پیش‌موجود در `test_voice_regressions` و
`test_premium_emoji` که نیازمند پکیجِ `telegram` هستند، بدون تغییر باقی ماند).

</div>

---

## نسخهٔ ۲.۲.۴ — 🧹 ممیزی سراسری منو‌ها: تعمیرات واقعی، چرخهٔ عمر سفارش، حذف دکمه‌های مرده

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۷

### ۱) حالت تعمیرات دیگر دور زده نمی‌شود 🛠

- **باگ:** نگهبان تعمیرات فقط `متن`، `کالبک` و دستور `/start` را می‌بست. بقیهٔ دستورات
  (`/help`، `/stop_order`، `/cancel` و ...) آزاد بودند و کاربر می‌توانست از این راه‌ها وارد جریان‌های
  ربات شود.
- **فیکس:** افزودن `MessageHandler(filters.COMMAND, _maintenance_guard)` در `group=-1`؛ امروز در حالت تعمیرات هیچ
  نوع آپدیتی (متن / دستور / دکمهٔ شیشه‌ای) جز برای سوپرادمین عبور نمی‌کند.
- **جاب زمان‌بندی هم آگاه شد:** در زمان تعمیرات هیچ سفارشی استارت
  نمی‌خورد (قبلاً در میانهٔ آپدیت اجرا می‌شدند) و پس از خاموش شدن،
  سفارش‌ها با سقف ۳ عدد در هر دوره اجرا می‌شوند تا یک‌باره ده‌ها
  سفارش روی هم ریخته نشود (همان سناریوی «همهٔ سفارشات لغو شدن»).

### ۲) چرخهٔ عمر سفارش: بزرگ‌ترین باگ‌ها چرا دیده نمی‌شدند 📦

- **باگ اصلی (لغوی بی‌اثر):** `settle_and_refund_order` وضعیت سفارش را در دیتابیس عوض
  نمی‌کرد مگر این‌که سفارش در حافظه باشد. نتیجه: لغوِ
  سفارش زمان‌بندی‌شده پول را برمی‌گرداند ولی سفارش همچنان
  `scheduled` می‌ماند و جاب زمان‌بندی آن را بعداً اجرا می‌کرد!
- **فیکس ساختاری:** افزودن `DatabaseManager.finalize_order_status()` — انتقال اتمیک که فقط
  از وضعیت‌های باز (`pending`/`running`/`scheduled`) عبور می‌کند. همهٔ مسیرهای لغو اول
  «ادعا» می‌کنند و بعد سراغ عودت می‌روند → نه عودت دوباره‌ای، نه
  اجرای دوباره، نه سفارش زامبی.
- `stop_active_order` حالا همیشه وضعیت را می‌بندد (قبلاً فقط وقتی که کالی فعالی پیدا می‌کرد).
- `_fail_order` دیگر سفارشی که به‌عنوان stopped/completed بسته شده را به `failed` برنمی‌گرداند.

### ۳) سفارش‌های «در صف» (pending) بالاخره دیده و قابل لغو شدند ⏳

- لغو توسط کاربر، لغو از پروفایل کاربر در پنل ادمین، و فلوی «لغوِ سفارش
  فعال» همگی `pending` را هم می‌بینند (قبلاً پیام «هیچ سفارش فعالی
  ندارد» می‌دادند).
- سفارش در صف مثل رزرو تسویه می‌شود: **عودت کامل** (قبلاً به اشتباه مشمول
  محاسبهٔ ثانیه‌ای می‌شد و از زمان انتظار کاربر پول کسر می‌گرفت).

### ۴) ری‌استارت دیگر پول کسی را نمی‌سوزاند ♻️

- `reset_stuck_orders` فهرست سفارش‌های نیمه‌کاره را برمی‌گرداند و جدید
  `startup_recovery_job` ماندهٔ مبلغ را با همان مرجع محاسبهٔ لغو به کیف پول عودت
  می‌دهد و به کاربر اطلاع می‌دهد.
- سفارش‌های `pending`ی که بیش از ۱۰ دقیقه در صف مانده‌اند، خودکار
  بسته و کاملاً عودت داده می‌شوند (آزاد شدن ظرفیت در
  گارد ظرفیت هم هست).

### ۵) آمار دقیق‌تر 📊

- افزودن شمارندهٔ `متوقف‌شده (لغو)` و `ناموفق` به آمار سفارشات.
- **مرز روز اصلاح شد:** «امروز» حالا به وقت تهران حساب می‌شود؛ قبلاً مبنای UTC
  بود و سفارش‌های نیمه‌شب تهران اشتباه شمرده می‌شدند.
- «📊 گزارش کلی» هم همهٔ وضعیت‌ها را نشان می‌دهد و گروندهٔ شکستن
  خطوطش (چاپ `ن` در متن) برطرف شد.

### ۶) دکمه‌های مرده حذف شدند 🧹

یک ممیزی خودکار روی همهٔ `callback_data`ها در برابر الگوهای ثبت‌شده انجام شد:

- `confirm_delete_dead` و `back_to_acc_menu` (گزارش سلامت اکانت‌ها) ثبت شدند — قبلاً بی‌پاسخ بودند.
- `back_to_reseller_menu` و `back_to_reseller_list` به الگوی هندلر نمایندگی‌ها اضافه شدند.
- دکمهٔ `noop` (شمارهٔ صفحه) حالا پاسخ می‌گیرد تا آیکون لودینگ تلگرام
  تا ۶۰ ثانیه نچرخد (کاربر خیال نکند ربات هنگ کرده).

### ❌ پوشش تست

- `tests/test_menu_and_lifecycle_fixes.py` — ۳۵ تست آفلاین جدید که هر شش دستهٔ بالا را قفل می‌کنند؛
  از جمله تستی که مطمئن می‌کند هیچ `callback_data`ی بدون هندلر نمی‌ماند.

</div>

---

## نسخهٔ ۲.۲.۳ — 🧰 فیکس منوها: تعمیرات سراسری، آمار کامل، لغو از پنل ادمین

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۷

### ۱) حالت تعمیرات حالا واقعاً همهٔ ربات‌ها را می‌بندد 🌍
- **باگ:** تاگل تعمیرات فقط `bot_data` همان اپی را که ادمین از طریق آن تاگل
  می‌کرد به‌روز می‌کرد؛ ربات‌های نمایندگی (اپ‌های جدا) پرچم قدیمی (خاموش)
  داشتند و کاربرانشان می‌توانستند سفارش بزنند.
- **فیکس:** تنظیم مرجع در `bot_id=1` ذخیره و پرچم همهٔ اپ‌های فعال همان لحظه
  فلیپ می‌شود؛ `bot_manager` هم در استارتاپ هر ربات، پرچم سراسری را می‌خواند.
  یعنی یک کلید = توقف همهٔ ربات‌ها + پایدار بعد از ری‌استارت.

### ۲) سفارش تازهٔ ثبت‌شده در آمار دیده نمی‌شد 📊
- **باگ:** `get_all_order_stats` فقط `running` و `scheduled` را می‌شمرد؛
  سفارش تازه در وضعیت `pending` (صف) بود و در هیچ آماری ظاهر نمی‌شد.
- **فیکس:** آمار حالا «⏳ در صف اجرا»، «📥 امروز» و «✅ تکمیل‌شده» را هم
  نشان می‌دهد (هم «📉 آمار کل ربات» و هم «📊 گزارش کلی»)؛ حذف پیام
  «در حال جمع‌آوری» هم ضدخطا شد (قبلاً خطای delete کل نمایش آمار را می‌خواباند).
- تاریخچهٔ کاربر هم وضعیت `pending` را درست نمایش می‌دهد («⏳ در صف»).

### ۳) لغو سفارش مستقیماً از لیست‌های ادمین 🛑
- **باگ:** در لیست سفارشات ادمین هیچ دکمه‌ای برای انتخاب و لغو هر سفارش
  نبود؛ تنها راه، فلو غیرشهودی «شمارهٔ ردیف بفرست» بود.
- **فیکس:** زیر «📦 سفارشات اخیر/زمان‌بندی‌شده» و «سفارشات کاربر» در پروفایل،
  برای هر سفارشِ قابل‌لغو دکمهٔ «🛑 لغو سفارش #id» اضافه شد → پیش‌نمایش مالی
  (کل/مصرف‌شده/قابل‌عودت) → لغو با عودت / بدون عودت / انصراف.
- سفارش‌های `pending` هم به فهرست «فعال» لیست‌های ادمین اضافه شدند تا
  قابل انتخاب و لغو باشند.

</div>

---

## نسخهٔ ۲.۲.۲ — 🛡 گارد ظرفیت منابع + بازگردانی گزارش مالی ادمین‌ها

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۷

### ۱) گارد ظرفیت منابع قبل از ثبت سفارش (آنی و زمان‌بندی‌شده)

**مشکل:** اگر در یک بازهٔ زمانی، مجموع سفارش‌های فعال/رزروی از ظرفیت اکانت‌های
سالم عبور می‌کرد، اجرا دچار کمبود منبع می‌شد (join ناموفق، سقوط live و در
بدترین حالت لغو زنجیره‌ای سفارش‌ها) و کاربر تا لحظهٔ اجرا بی‌خبر بود.

**راه‌حل (`services/capacity_planner.py`):**
- قبل از پذیرش هر سفارش، مصرف منابع در **کل بازهٔ اجرا** `[شروع، شروع+مدت]`
  سنجیده می‌شود — نه فقط لحظهٔ شروع. پس اگر ساعت ۱۲:۰۰ شلوغ باشد و کاربر
  ۱۲:۱۰ تلاش کند، سفارشش پذیرفته **نمی‌شود**؛ سیستم اولین شروعی را که
  «کل مدت سفارش» در آن جا می‌شود دقیق محاسبه و پیشنهاد می‌کند (مثلاً ۱۳:۳۵).
- پیشنهاد با یک دکمه «📅 رزرو در ساعت پیشنهادی» قابل ثبت است + دکمه
  «🔄 بررسی مجدد ظرفیت».
- پیش‌نمایش زندهٔ ظرفیت در صفحهٔ تأیید سفارش (🟢 موجود / 🔴 تکمیل) قبل از پرداخت.
- محاسبهٔ اوج مصرف به‌صورت event-based و دقیق (نه نمونه‌گیری زمانی)؛
  سفارش‌های «تکمیل و خروج» (بدون مدت) با تخمین امن لحاظ می‌شوند.
- حاشیهٔ ایمن پیش‌فرض ۱۰٪ از پول اکانت‌ها (`CAPACITY_SAFETY_BUFFER_PERCENT`)
  و سقف سفارش همزمان (`MAX_CONCURRENT_ORDERS`) هم اعمال می‌شود.
- **Fail-open:** هر خطای داخلی گارد، مسیر خرید را نمی‌بندد (فقط لاگ).
- ۲۱ تست آفلاین جدید (`tests/test_capacity_planner.py`).

### ۲) بازگردانی گزارش افزایش/کاهش موجودی ادمین‌ها در کانال گزارشات پرداختی

**مشکل:** شارژ/کسر موجودی کاربران توسط ادمین‌ها فقط به ادمین و کاربر اطلاع
داده می‌شد و هیچ ردپایی در «کانال گزارشات پرداختی» (`log_channel_payments`)
نمی‌ماند.

**راه‌حل (`services/payment_reporter.py`):**
- هر تغییر موجودی توسط ادمین، گزارش شکیل (مبلغ، کاربر، ادمین عامل، موجودی
  جدید، ربات، توضیح و زمان شمسی) به همان کانالی می‌رود که رسیدهای پرداخت
  آنلاین می‌روند — همهٔ جریان مالی در یک‌جا.
- ارسال با Markdown + fallback متن ساده؛ هرگز عملیات مالی ادمین را نمی‌شکند.
- تست‌های آفلاین (`tests/test_payment_reporter.py`).

### متفرقه
- `BOT_VERSION` به `2.2.2` بروزرسانی شد (برچسب قدیمیِ جا ماندهٔ 2.2.1).
- متغیرهای محیطی جدید در `.env.example` مستند شدند.

</div>

---

## نسخهٔ ۲.۲.۱ — 🚪 خروج مدیریت‌شده از ویس‌کال (ضد burst)

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۱

### مشکل
بعد از **پایان** یا **لغو** سفارش، همهٔ اکانت‌ها یک‌جا (هم‌زمان با `asyncio.gather`)
از ویس‌کال و گروه خارج می‌شدند. این burst شبیه رفتار ربات است و می‌تواند
FloodWait / محدودیت اکانت ایجاد کند.

### فیکس
- `VoiceCallManager.stop_all_for_order` و `cleanup_all` با **فاصلهٔ پلکانی**
  (`VOICE_LEAVE_STAGGER_MIN/MAX` پیش‌فرض ۰.۸–۱.۵ ثانیه) و **سقف هم‌زمانی**
  (`VOICE_LEAVE_MAX_CONCURRENCY=2`) اکانت‌ها را خارج می‌کنند.
- مانیتور سفارش **قبل** از leave متوقف می‌شود تا وسط خروج دوباره join نکند.
- `OrderExecutor._eject_all_fast` / `_cleanup_order` همان pacing را برای
  group/channel و leftoverها اعمال می‌کنند (بدون double-leave روی voice).
- ترتیب leave تصادفی می‌شود تا fingerprint ثابت نداشته باشد.
- کلیدهای env جدید در `.env.example` و `config.py`.

</div>

---

## نسخهٔ ۲.۲ — 🎨 UI رنگی + فیکس ایموجی پریمیوم

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۱ · **Bot API 9.4**

### 🐛 باگ‌فیکس‌های حیاتی

- **ایموجی پریمیوم بعد از یک خطا برای همیشه خاموش می‌شد:** خطای
  `Can't parse entities: unclosed end tag` (ناشی از برش وسط تگ `<tg-emoji>`
  در پیش‌نمایش بسته) کل چت ادمین را `mark_unsupported` می‌کرد و حتی با
  روشن‌بودن سوئیچ‌ها دیگر ایموجی پریمیوم ارسال نمی‌شد. حالا فقط خطاهای
  واقعیِ «custom emoji مجاز نیست» چت را blacklist می‌کنند؛ خطاهای parse
  با sanitize + fallback درمان می‌شوند و چت سالم می‌ماند.
- **پیش‌نمایش بسته:** پیام‌ها چندتکه (۲۵تایی) و با `safe_html_truncate`
  فرستاده می‌شوند تا تگ وسط برش نخورد و سقف ۱۰۰ entity رعایت شود.
- **کشف از اکانت (MTProto):** پارامتر درست `stickerset` (نه `sticker_set`)
  برای kurigram/Layer جدید — خطای `GetStickerSet unexpected keyword` رفع شد.
- **بازنشانی blacklist در استارت** و دکمهٔ «♻️ بازنشانی چت‌های مسدود» در پنل.

### ✨ قابلیت‌های جدید UI

- **دکمه‌های رنگی Bot API 9.4** روی همهٔ inline/reply:
  سبز (`success`) = تایید/پرداخت/فعال · قرمز (`danger`) = حذف/انصراف/رد ·
  آبی (`primary`) = بازگشت/ادامه/تنظیمات. تشخیص خودکار از متن دکمه
  (`classify_button_style`) + امکان تنظیم دستی.
- **HTML ایمن:** `safe_html_truncate` / `sanitize_html` / `strip_tg_emoji_tags`
  + `blockquote` / `expandable` / `divider` / `section_title` برای UI تمیز.
- **خوش‌آمدگویی HTML مینیمال** با موجودی فرمت‌شده و کیبورد پایدار.
- کلید env جدید: `PREMIUM_EMOJI_COLORED_BUTTONS=true`

</div>

---

## نسخهٔ ۲.۱ — 💎 ایموجی پریمیوم (Custom Emoji)

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۱ · **مبتنی بر Bot API 9.4 (۹ فوریهٔ ۲۰۲۶)**

از این نسخه، اگر اکانتِ **مالکِ ربات** اشتراک Telegram Premium داشته باشد،
کل خروجی‌های ربات (منوها، متن‌ها، دکمه‌ها، تیکت، بازنشر پیام کاربر) با
ایموجی پریمیوم/انیمیشنی ارسال می‌شود. راهنمای کامل:
[`docs/premium-emoji.fa.md`](docs/premium-emoji.fa.md)

### ✨ قابلیت‌های جدید

- **لایهٔ ایموجی پریمیوم در خروجیِ ربات:** کلاس‌های `PremiumEmojiBot`
  (زیرکلاسِ `ExtBot`) و `PremiumEmojiApplication` در `utils/premium_bot.py`
  همهٔ متدهای `send_message` / `edit_message_text` / `send_photo` /
  `send_video` / `send_document` / `send_animation` / `send_audio` /
  `send_voice` / `send_media_group` را ارتقا می‌دهند. چون در PTB همهٔ
  مسیرهای ارسال (مثل `context.bot`، `message.reply_text`،
  `callback_query.edit_message_text`، `send_safe`) در نهایت به همین متدها
  می‌رسند، **هیچ هندلری نیاز به تغییر نداشت** و همهٔ متن‌های موجود خودکار
  ارتقا می‌یابند.
- **ارتقای هوشمند متن در هر parse_mode:**
  - HTML → تبدیل ایموجی به `<tg-emoji emoji-id="…">` (بدون تگ تودرتو:
    داخل `<code>`/`<pre>`/`<tg-emoji>` موجود و داخل attributeها دست
    نمی‌خورد)
  - بدون parse_mode → افزودن entity از نوع `custom_emoji` با آفست‌های دقیقِ
    UTF-16 (برای متن فارسی هم درست است)
  - Markdown → تبدیل به HTML (`**بولد**` → `<b>بولد</b>`) و سپس ارتقا
  - سقف ۹۰ ایموجی در هر پیام (تلگرام حداکثر ۱۰۰ entity می‌پذیرد)
- **آیکون پریمیوم روی دکمه‌ها:** ایموجی اول/آخرِ متنِ دکمه‌های **inline** و
  **کیبورد اصلی (reply)** به فیلد رسمی `icon_custom_emoji_id` تبدیل و از
  متن حذف می‌شود (آیکون قبل از متن رندر می‌شود؛ بدون این کار دو ایموجی
  دیده می‌شد).
- **لایهٔ بازگردانی برچسب دکمه‌ها:** هر برچسبی که ایموجی‌اش حذف شده در یک
  نگاشت ثبت می‌شود و `PremiumEmojiApplication.process_update` پیش از
  dispatch هر آپدیت، متنِ دریافتی را عیناً به شکل اصلی برمی‌گرداند و آفست
  entityها را هم اصلاح می‌کند. نتیجه: ده‌ها فیلتر دقیق مثل
  `filters.Regex("^🆘 پشتیبانی$")` و همهٔ `ConversationHandler`ها **بدون
  هیچ تغییری** درست کار می‌کنند.
- **بازنشر ایموجی پریمیومِ خودِ کاربر:** در تیکت (پیام کاربر → ادمین و
  پاسخ ادمین → کاربر، شامل کپشن عکس/ویدئو/ویس/فایل) با `text_html` همان
  ایموجی سفارشیِ کاربر منتقل می‌شود. پیام همگانی و پیام خصوصیِ ادمین با
  `copy_message` / `message.copy()` به‌صورت ذاتی همهٔ entityها را حفظ
  می‌کنند.
- **بستهٔ آمادهٔ شناسه‌ها:** ۱۶۳ کلید معنایی (`check`, `cross`, `rocket`,
  `wallet`, `support`, `settings`, `voice`, `clock`, …) + نگاشتِ
  «یونیکد → شناسه» برای **۳۸۱** ایموجی (با و بدون Variation Selector) به
  انضمام صدها معادلِ دستی؛ پوششِ کاملِ ۱۵۳ ایموجی یونیکدِ موجود در متن‌های
  ربات. (`utils/premium_emoji.py`)
- **اعتبارسنجی خودکار در هر استارت:** همهٔ شناسه‌ها با متد رسمی
  `getCustomEmojiStickers` سنجیده می‌شوند (هر ۲۰۰ شناسه یک فراخوانی)؛
  شناسهٔ نامعتبر از مدار خارج شده و ایموجی یونیکد جای آن را می‌گیرد.
- **پنل ادمینِ ایموجی پریمیوم:** `⚙️ تنظیمات سیستم → 💎 ایموجی پریمیوم`
  شامل چهار کلید روشن/خاموش (ذخیره در دیتابیس)، «🧪 پیام تست»، «📋
  پیش‌نمایش بسته»، «🔄 اعتبارسنجی شناسه‌ها»، «🔧 جایگزینی شناسه» و «🛜
  کشف از اکانت (MTProto)» برای خواندن پک‌های ایموجی دلخواه.
  (`handlers/premium_emoji_handlers.py`)
- **سرویس پایدارِ تنظیمات:** کلیدهای تنظیم در دیتابیس ذخیره می‌شوند و بر
  `.env` اولویت دارند. (`services/premium_emoji_service.py`)
- **ابزار خط فرمان:** `python tools/premium_emoji_sync.py --list |
  --validate | --discover PACKS` برای اعتبارسنجی و کشف شناسه‌ها بیرون از
  ربات، با خروجی آماده برای `.env`.
- **fallback خودکار و تفکیک چندرباتیه:** اگر تلگرام payload ارتقایافته را
  نپذیرد (کانال، مالک بدون پریمیوم، شناسهٔ نامعتبر)، بلافاصله همان پیام با
  payload اصلی ارسال می‌شود و آن چت — به‌تفکیک توکن هر ربات — از فهرست
  ارتقا خارج می‌شود تا دوباره تلاش نشود. ربات‌های نمایندگی هم همین لایه را
  دارند. (`services/bot_manager.py`)

### 🐛 رفع اشکال‌های مهم

- **رفع شکستن HTML در تیکت:** متن پیام کاربر، موضوع تیکت و نام‌ها که از
  دیتابیس می‌آمدند بدون escape داخل پیام HTML قرار می‌گرفتند و هر متن
  حاوی `<` یا `&` باعث `Can't parse entities` و از دست رفتن اعلان می‌شد.
  اکنون همهٔ محتوای دیتابیس با `escape_outside_tags` امن می‌شود و تنها
  تگ‌های ساختهٔ خودِ ربات سالم می‌مانند.
- **رفع بریدن وسط تگ در پیش‌نمایش:** بریدن متنِ HTML‌شده در کاراکترِ ثابت
  می‌توانست یک `<tg-emoji>` را نصف کند؛ اکنون برای پیام‌های کوتاه (≤۶۰
  کاراکتر) خودِ ایموجی پریمیومِ کاربر نمایش داده می‌شود و برای پیام‌های
  بلند، پیش‌نمایشِ متنیِ escape‌شده.
- **رفع گم‌شدن ایموجی پریمیوم در پاسخ ادمین:** پاسخ ادمین به کاربر حالا با
  `parse_mode="HTML"` و `message_content_html` فرستاده می‌شود تا ایموجی
  سفارشیِ ادمین (در متن و کپشن عکس/ویدئو/ویس/فایل) به کاربر برسد.

### ⚙️ تغییرات فنی

- **تغییر مسیرِ parse_mode:** پیام‌هایی که قبلاً با `parse_mode=MARKDOWN`
  (پیش‌فرض `send_safe`) ارسال می‌شدند، حالا به HTML تبدیل می‌شوند؛ این
  تبدیل دقیقاً با قواعد Markdown قدیمیِ تلگرام هم‌خوان است
  (`*بولد*`/`**بولد**` → `<b>`، `_ایتالیک_` → `<i>`، `` `code` `` →
  `<code>`، ```` ```pre``` ```` → `<pre>`، `[متن](لینک)` → `<a href>`) و با
  `PREMIUM_EMOJI_MARKDOWN_TO_HTML=false` قابل خاموش کردن است.
- **نسخهٔ ربات:** ثابت `BOT_VERSION = "2.1.0"` در `constants.py` اضافه شد و
  در لاگ استارت و پنل ایموجی پریمیوم نمایش داده می‌شود.
- **کلیدهای `.env` جدید:** `PREMIUM_EMOJI_ENABLED`, `PREMIUM_EMOJI_TEXT`,
  `PREMIUM_EMOJI_BUTTONS`, `PREMIUM_EMOJI_REPLY_BUTTONS`,
  `PREMIUM_EMOJI_TRAILING_ICONS`, `PREMIUM_EMOJI_MARKDOWN_TO_HTML`,
  `PREMIUM_EMOJI_VALIDATE`, `PREMIUM_EMOJI_SKIP_CHANNELS`,
  `PREMIUM_EMOJI_MAX_PER_MESSAGE`, `PREMIUM_EMOJI_OVERRIDES`.
- **۶۱ تست آفلاین جدید** در `tests/test_premium_emoji.py` (بدون هیچ تماس
  شبکه‌ای): بستهٔ شناسه‌ها، آفست UTF-16، ارتقای HTML، تبدیل Markdown،
  ارتقای markup، بازگردانی برچسب، بازنشر محتوای کاربر، کل خط لولهٔ بات
  شامل fallback و سقف entity، و مسیر اعتبارسنجی/کشف نامربوطی ایموجی.
- **کنترلِ «ایموجی نامربوط»:** `getCustomEmojiStickers` فیلد `Sticker.emoji`
  را هم برمی‌گرداند؛ اگر ایموجیِ واقعیِ یک شناسه با ایموجیِ انتظارِ بسته
  نخواند، در لاگ و پنل ادمین گزارش می‌شود و با
  `PREMIUM_EMOJI_STRICT_MATCH=true` خودکار غیرفعال هم می‌شود.
- **پوشش کامل‌تر نگاشت یونیکد:** با اسکن خودکارِ کل مخزن، ۳۰ معادلِ دیگر
  (🔸 🔹 🔻 ⬜️ 🔟 🏁 ♾ 📆 👮‍♂️ 🏳️ 📁 🚦 📜 ⏭ ⚰️ 🔋 🎉 🔘) به نگاشت اضافه شد تا
  همهٔ ایموجی‌های یونیکدِ متن‌های ربات (به‌جز پیکان‌های تایپی، پرچم و
  ری‌اکشن‌های داخل ویس‌کال) ارتقا یابند.

### 🔎 محدودیت‌های تلگرام (طبیعی، نه باگ)

- ایموجی سفارشی در **کانال** مجاز نیست (فقط خصوصی/گروه/سوپرگروه) → کانال‌ها
  خودکار رد می‌شوند.
- `answerCallbackQuery` (toast دکمه‌ها) و `setMyCommands` از entity/HTML
  پشتیبانی نمی‌کنند → ایموجی یونیکد می‌مانند.
- پیش‌نیاز: اشتراک **Telegram Premium** روی اکانتِ سازندهٔ ربات در BotFather.

</div>

---

## نسخهٔ ۲.۰ — چت داخل تماس + پایداری لغو سفارش

<div dir="rtl">

### ✨ قابلیت‌های جدید

- **مرکز چت داخل تماس (In-Call Chat):** مشتری می‌تواند از اکانت‌های فعالِ یک
  سفارش، به‌صورت تک یا گروهی (multi-select)، کامنت یا ری‌اکشن بفرستد. منو
  «قابل استفادهٔ مکرر» است: تا وقتی سفارش فعال است می‌توان بارها پیام/ری‌اکشن
  فرستاد. (`handlers/incall_handlers.py`)
- **ارسال واقعی پیام/ری‌اکشن در ویس‌کال:** با ارتقا به **kurigram** (fork
  نگهداری‌شدهٔ Pyrogram با Layer جدید تلگرام) متد
  `phone.sendGroupCallMessage` واقعاً کار می‌کند.
- **کلید روشن/خاموش برای ادمین:** قابلیت چت داخل تماس از پنل ادمین
  فعال/غیرفعال می‌شود (کلید تنظیم: `service_incall_chat`).
- **انتخاب سفارش هوشمند:** اگر مشتری چند سفارش فعال داشته باشد، ابتدا سفارش
  موردنظر را انتخاب می‌کند.

### 🐛 رفع اشکال‌های مهم

- **رفع خرابیِ کاملِ ورود اکانت‌ها به ویس‌کال:** پس از سوییچ به kurigram،
  یک monkeypatch قدیمی به ثابتِ حذف‌شدهٔ `pyrogram.utils.MIN_CHAT_ID` اشاره
  می‌کرد و باعث می‌شد **هیچ اکانتی** نتواند وارد تماس شود. اکنون این patch
  فقط روی Pyrogram کلاسیک اجرا می‌شود و روی kurigram (که خودش شناسه‌های
  مدرن کانال را پشتیبانی می‌کند) رد می‌شود. (`services/voice_call_manager.py`)
- **رفع burst در ارسال پیام/ری‌اکشن گروهی:** پیش‌تر همهٔ ارسال‌ها در یک لحظه
  شلیک می‌شد (`asyncio.gather`) و باعث FloodWait و دیده‌نشدن پیام می‌شد.
  اکنون ارسال‌ها با فاصلهٔ منظم (~۱ درخواست در ثانیه) و سقف هم‌زمانی محدود
  انجام می‌شوند: نه یک‌دفعه‌ای، نه کند. قابل تنظیم با
  `INCALL_SEND_STAGGER_MIN/MAX` و `INCALL_SEND_MAX_CONCURRENCY`.
- **رفع گزارش لغوِ تکراری/ناقص:** هنگام لغو سفارش، دو گزارش به کانال می‌رفت
  (یکی کامل، یکی ناقص با «—»). اکنون فقط **یک گزارش کامل و درست** ارسال
  می‌شود. (flag یک‌بارمصرفِ سرکوب گزارش در `services/order_executor.py`)
- **رفع `CHANNEL_INVALID` در ارسال چنداکانتی:** `access_hash` کانال برای هر
  اکانت متفاوت است؛ اکنون peer با سشن همان اکانتِ فرستنده resolve می‌شود.
- **رفع `GROUPCALL_INVALID`:** رفرش خودکارِ رفرنسِ کهنهٔ تماس هنگام ارسال.
- **رفع اسپمِ healthcheck پستگرس:** افزودن `-d` به `pg_isready`.

### ⚙️ تغییرات فنی

- **کتابخانهٔ MTProto:** جایگزینی Pyrogram با `kurigram>=2.2.25` (با نام
  `pyrogram` ایمپورت می‌شود؛ drop-in). سشن‌استرینگ‌های قبلی معتبر می‌مانند.
  `py-tgcalls==2.2.5` بدون تغییر کار می‌کند.
- **دکمه‌های بازگشت:** شبکهٔ ایمنیِ سراسری برای دکمهٔ «🔙 بازگشت» تا هیچ دکمه‌ای
  بی‌اثر نماند.

</div>

---

## نسخه‌های پیشین (خلاصه)

<div dir="rtl">

- **پنل اکانت‌ها (glass UI):** بازطراحی فهرست اکانت‌ها و منوهای ویرایش با
  دکمه‌های شیشه‌ای inline، بدون «آیدی را تایپ کنید».
- **پرداخت چنددرگاهی:** پشتیبانی هم‌زمان از **زرین‌پال** و **آقای پرداخت** +
  منوی انتخاب درگاه؛ عبور درخواست‌های درگاه از پروکسیِ بدون WARP.
- **تیکتینگ حرفه‌ای:** بازطراحی کامل سیستم تیکت + بستن خودکار.
- **گزارش‌های مالی دقیق:** تسویهٔ ثانیه‌ای (pro-rated) و انتخاب عودت/عدم‌عودت
  برای ادمین.
- **زیرساخت WARP ایزوله:** کل ترافیک ربات از کانتینر WARP عبور می‌کند تا
  مدیای صوتی (UDP) پایدار بماند، بدون لمس SSH/شبکهٔ هاست.
- **بهینه‌سازی CPU:** ffmpeg تک‌ترد، صدای سکوت مونو ۲۴kHz، لاگ‌های کم‌حجم،
  uvloop.
- **Adaptive Join Brain:** مدیریت هوشمند هم‌زمانی و backoff برای کاهش
  FloodWait هنگام ورود اکانت‌ها.

</div>

---

<div dir="rtl">

راهنمای کامل راه‌اندازی روی سرور جدید:
[`docs/new-server-setup.fa.md`](docs/new-server-setup.fa.md)

</div>
