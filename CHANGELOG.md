<div dir="rtl">

# تاریخچهٔ تغییرات (Changelog)

این فایل مهم‌ترین تغییرات پروژه را به‌ترتیب نسخه فهرست می‌کند.
تاریخ‌ها شمسی و توضیحات فارسی هستند (اصطلاحات فنی به انگلیسی).

</div>

---

## نسخهٔ ۲.۳.۱۷ — بررسی مرحله‌ایِ سشن سوخته و حذف همهٔ موارد اثبات‌شده

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۲ — کد شاخهٔ `arena/01a0ccf5-tgcallbot`؛ وضعیت واقعی پردازه و حساب‌های سرور بعد از این تغییر هنوز تأیید نشده است.

- درخواست سوپرادمین برای «بررسی سشن‌های خارج‌شده یا حساب‌های دلیت‌شده و حذف آن‌ها از لیست/DB» پیاده شد. دکمهٔ «🗑 حذف همهٔ تأییدشده‌ها (۰)» **همیشه** روی منوی شیشه‌ای دیده می‌شود؛ وقتی صفر است چیزی حذف نمی‌شود و مسیر بررسی پیشنهاد می‌شود. «🧪 بررسی مرحله‌ای همهٔ سوخته‌های غیرفعال» پیش‌نمایش/تأیید یک‌بارمصرف، وضعیت، توقف و گزارش نتیجه دارد؛ کار در همان پردازهٔ ربات و فقط **یکی‌یکی** انجام می‌شود، نه یک اتصال/حذف دسته‌جمعیِ بی‌گارد.
- بررسی با تعمیرات سراسری، نبود سفارش فعال/نزدیک (قبل و درست پیش از هر اتصال)، سقف ۱۰۰ نامزد/۵۰۰۰ ردیف، تفکیک auth-key واقعی در ربات‌های اصلی/نمایندگی، رد سشن ناخوانا/مشترک، کنترل مهلت ۴۰۶ و fingerprint همان ciphertext محدود است. ۴۰۶ `active` فقط مسیر تک‌اکانتی دارد. پروب سالم پس از قطع قطعی می‌تواند همان حساب را فعال کند. اتصال نامطمئن، ۴۰۶، ban، متن تاریخی یا ۴۰۱ عمومی هیچ مجوز حذفی نمی‌سازند.
- دو نشانگر متمایز فقط پس از پاسخ تایپ‌شده و قطع تأییدشده ساخته می‌شوند: `USER_DEACTIVATED` برای **حذف حساب** و `SESSION_REVOKED`/`SESSION_EXPIRED`/`AUTH_KEY_UNREGISTERED`/`AUTH_KEY_INVALID` برای **غیرقابل‌استفاده‌شدن همان سشن** (حساب تلگرام ممکن است با ورود تازه پابرجا باشد). خودِ بررسی ردیفی پاک نمی‌کند. حذف یک مورد یا همهٔ موارد اثبات‌شده پیش‌نمایش ID/نوع، تأیید دوم، بکاپ و تراکنش با بررسی دوبارهٔ وضعیت/ربات/سشن/نشانگر/تعمیرات/سفارش می‌خواهد. سشن‌های ۲۵ حساب تاریخی با برچسب صرف `inactive` خودکار حذف نمی‌شوند.
- **۳۷۵ آزمون آفلاین اجرا شد: ۳۷۴ موفق، ۱ skip**؛ SQLite و پروب‌های mock تفکیک RPC، جلوگیری از حذف قدیمی‌ها و کلید مشترک، توقف/expiry، مسیر منوی صفر تا حذف گروهی و حفظ سشن سالم را پوشش می‌دهند. این نتیجه تماس واقعی Telegram، وضعیت نسخهٔ درحال اجرای سرور یا نتیجهٔ مالی را اثبات نمی‌کند.

</div>

---

## نسخهٔ ۲.۳.۱۶ — منوی شیشه‌ای عملیاتی حذف حساب دلیت‌شده

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۲ — اصلاح روی شاخهٔ `arena/01a0ccf5-tgcallbot`؛ هنوز هیچ تست زندهٔ Telegram/DB سرور انجام نشده است.

- منوی سوپرادمین، حتی با شمار تأییدشدهٔ صفر، دکمه‌های شیشه‌ای انتخاب صفحه‌بندی‌شدهٔ حساب مشکوک، رفرش و بازگشت دارد؛ دیدن فهرست **هیچ اتصال زنده‌ای باز نمی‌کند**. پس از انتخاب فقط یک حساب، یک تأیید پنج‌دقیقه‌ای و یک‌بارمصرف بر اساس ربات/ادمین/چت/اثر انگشت سشن لازم است.
- بررسی تک‌اکانتی در همان پردازهٔ اصلی با قفل مالکیت سشن اجرا می‌شود؛ پیش از آن تعمیرات سراسری و نبود سفارش فعال/نزدیک کنترل می‌شوند. اگر حساب معتبر و اتصالش قطع شد، همان ردیف می‌تواند فعال شود؛ تنها پاسخ تایپ‌شدهٔ `USER_DEACTIVATED` با قطع تأییدشده آن را برای منوی حذف واجد شرایط می‌کند. **inactive/۴۰۶/SESSION_REVOKED تاریخی اجازهٔ حذف یا بررسی انبوه نیست.**
- حذف **فقط همان حساب بررسی‌شده** یا مجموعهٔ همهٔ حساب‌های تأییدشده با دکمهٔ مجزا، به پیش‌نمایش، تأیید دوم و تراکنش DB با بازبینی نشانگر/سشن/تعمیرات/سفارش محدود است. هیچ سشن تاریخی به‌طور خودکار حذف یا وضعیتش تغییر نمی‌کند. [`راهنمای ایمنی سشن`](docs/session-safety.fa.md) و [`دستورهای استقرار`](docs/deploy-final.fa.md) به‌روز شده‌اند.
- **۳۵۶ آزمون آفلاین اجرا شد: ۳۵۵ موفق، ۱ skip**؛ مسیر شیشه‌ایِ شمار صفر، صفحه‌بندی، انتخاب/تأیید تک‌اکانتی، شکست پیش‌شرط، تغییر کلید، RPC تایپ‌شده و حذف تراکنشی را پوشش می‌دهند. این‌ها **اثبات کارکرد منو در ربات زنده یا سالم‌بودن ۲۵ سشن سرور نیستند**.

</div>

---

## نسخهٔ ۲.۳.۱۵ — تکمیل سازگاری عملیاتی و ایمنی سفارش/سشن

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۲ — کد شاخهٔ `arena/01a0ccf5-tgcallbot`؛ نصب واقعی روی سرور و سلامت ۲۵ سشن تاریخی هنوز تأیید نشده است.

- ورود ترتیبیِ تک‌join در هر سفارش و ریکاوری، فاصلهٔ بعد از پایان ورود با اولویت ضداسپم، پیش‌گرم‌سازی اختیاری، شانس دوم محدود فقط برای خطاهای گذرا و توقف محافظه‌کارانهٔ timeout اضافه شد. سقف‌ها **تعداد حساب واقعی یا قیمت پلن را دستکاری نمی‌کنند**.
- `VOICE_SILENCE_MODE=media|auto|listener`، listener آزمایشی با تأیید مستقل حضور و binding و fallback به media، جمع‌آوری کلاینت‌های idle، گزارش RSS و حد نرم حافظه پیاده‌سازی شد؛ ۴۰۶ هرگز fallback join یا retry خودکار نمی‌شود.
- نشانگر قرنطینهٔ ۴۰۶ در DB ماندگار، بدون حذف ردیف/سشن است و از تخصیص جدید جلوگیری می‌کند. بررسی دستی تک‌اکانتی پس از cooldown، با تأیید هویت و قطع اتصال قطعی و compare-and-swap کلید/زمان نشانگر را برمی‌دارد؛ برچسب حذف واقعی `USER_DEACTIVATED` و منوی سوپرادمین از ۲.۳.۱۴ باقی‌اند.
- `ORDER_LINK_MODE` و اعتبارسنجی پیوند/مقصد یکسان (شامل ذخیرهٔ URL واقعی `text_link` پنهان)، سقف سراسری سفارش‌های pending/running در تراکنش خرید و claim زمان‌بندی‌شده، رد خرید بدون برداشت وجه در تعمیرات و fail-closed بارگذاری پرچم تعمیرات **پس از** بازیابی persistence اضافه شد. انتشار هم‌زمان ربات نمایندگی و تاگل تعمیرات هماهنگ‌اند. حد منابع Compose از `BOT_MEM_LIMIT`/`BOT_CPU_LIMIT` خوانده می‌شود.
- هنگام توقف تماس، idle reaper دیگر کلاینت را در میانهٔ خروج/پرس‌وجوی native قطع نمی‌کند؛ ردیف‌های زمان آخرین استفادهٔ کلاینت ناموجود در حافظه باقی نمی‌مانند. خطای HTML/پیوند پرداخت نمی‌تواند اسکریپت تزریق کند؛ هدایت فقط به میزبان‌های HTTPS دو درگاه رسمی محدود است. لینک‌های تاریخی ساخته‌شده توسط همین کدها سازگارند، اما لینک‌های ناشناختهٔ pending باید پیش از استقرار بررسی شوند.
- گارد استقرار به‌جای رد صرف وجود کلیدهای قدیمی، مقادیر عملیاتی را پیش از Docker/backup و بدون چاپ اسرار اعتبارسنجی می‌کند؛ اگر پردازهٔ اصلی ربات پس از `up` تازه شروع نشده باشد، نسخهٔ خوانده‌شده در `docker exec python` را به‌اشتباه موفقیت تلقی نمی‌کند.
- پس از پیش‌پرواز ناموفق سرور: گارد اکنون قبل از هر Docker **Git کثیف/فایل ناشناس** را رد و بعد از build دوباره کنترل می‌کند؛ پرداخت pending ایجادشده در ۳۰ دقیقهٔ اخیر یا با زمان نامعلوم نیز قبل و بعد از build مانع استقرار می‌شود. [راهنمای نصب](docs/deploy-final.fa.md) برای گزارش واقعی ۱۴ فایل عادیِ **صفر بایتی** (حتی dump نامعتبرِ صفر بایتی) فرمان قرنطینهٔ **بدون prompt**، با whitelist دقیق و حفظ فایل‌ها بیرون پروژه دارد؛ prompt قدیمی در paste چندخطی تأیید را مصرف می‌کرد و هیچ فایلی منتقل نمی‌کرد. ممیزی فقط‌خواندنی نشان داد ۷۷ پرداخت pending همگی قدیمی‌تر از ۲۴ ساعت‌اند، ۵۲ مورد زرین‌پال URL ندارند، پرداخت تازه صفر است، و ledger جدید برای سفارش ۸۴۶ ردیفی نشان نداده است؛ **این‌ها اصلاح/تسویه نشده‌اند** و هنوز به تأیید مالی نیاز دارند. **۳۴۱ آزمون آفلاین اجرا شد: ۳۴۰ موفق، ۱ skip**؛ بررسی نحوی و گارد `.env.example` هم گذشتند. **این نتیجه اتصال واقعی Telegram/UDP، وضعیت سفارش/ledger یا اعتبار سشن‌های سرور را ثابت نمی‌کند**. دستورات نصب فقط پس از بررسی انسانی پیش‌شرط‌ها قابل اجرا هستند.

</div>

---

## پیش‌نویس ۲.۳.۱۴ — گارد استقرار و ممیزی سازگاری

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱ — مبنای این نسخه ۲.۳.۱۲ است؛ حفظ فایل‌ها و توابع آن **به معنای پشتیبانی قابلیت‌های شاخه‌های تاریخی دیگر نیست**. گزینه‌های مهمی از `.env` عملیاتی (ورود ترتیبی، شانس دوم و listener) هنوز در این شاخه اجرا نمی‌شوند.

- [هشدار سازگاری](docs/env-compatibility.fa.md) و گاردی اضافه شد که اگر این تنظیمات در `.env` وجود داشته باشند، قبل از Docker/backup و بدون چاپ مقدارها استقرار را رد می‌کند. تنظیمات را فقط برای دور زدن گارد حذف نکنید.
- [پیش‌نویس راهنمای استقرار](docs/deploy-final.fa.md) اضافه شد؛
  مسیرهای قدیمی `git pull main` و شاخه‌های قدیمی از راهنماهای نصب اصلاح شدند.
- `deploy-warp.sh` دیگر قبل از اجرا `compose down` نمی‌زند و فرمان `down`
  را هم رد می‌کند. فقط پس از تأیید اپراتور، فعال‌بودن حالت تعمیرات در DB و
  **صفر بودن سفارش‌های `running`/`pending` و رزرو نزدیک** اجازهٔ استقرار دارد؛
  قبل از build بکاپِ قابل‌خواندن با دسترسی خصوصی **بیرون از پوشهٔ پروژه**
  می‌گیرد و پس از build دوباره وضعیت را چک می‌کند. در صورت خطا بدون خاموشی
  خودکار متوقف می‌شود. این گارد جای بررسی زندهٔ سفارش ۸۴۶ را نمی‌گیرد.
- Docker build دیگر backupهای محلی یا فایل‌های تنظیمات خصوصی را کپی نمی‌کند.
  نصب روی سرور واقعی انجام نشده است؛ تست آفلاینِ گارد استقرار جای سلامت تماس
  و سشن‌های واقعی را نمی‌گیرد.
- منوی تازهٔ سوپرادمین برای **فقط حذف اکانت‌هایی که تلگرام حذف حسابشان را
  صریحاً تأیید کرده** اضافه شد. نشانگر جدید فقط از `USER_DEACTIVATED` تایپ‌شده
  در بررسی زندهٔ تک‌اکانتی با قطع تأییدشده ساخته می‌شود؛ `BAN`، خطای ۴۰۶ و
  برچسب‌های تاریخی ۲۵ سشن غیرفعال واجد حذف انبوه نیستند. پیش‌نمایش/تأیید
  یک‌بارمصرف و بازبینی تراکنشی در حالت تعمیرات لازم است. کالبک حذفِ انبوه
  قدیمی و غیرفعال‌بودنِ آن حفظ شده است؛ [جزئیات](docs/session-safety.fa.md).

</div>

---

## نسخهٔ ۲.۳.۱۳ — تحویل شاخهٔ کامل ۲.۳.۱۲ همراه با اصلاحات سشن و تسویه

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱ — بر پایهٔ commit `0a627e1` شاخهٔ ۲.۳.۱۲، نه شاخهٔ قدیمی `main` (۲.۲.۳).

- **حفظ امکانات:** ضداسپم، خروج زمان‌بندی‌شده، منو و لغو سفارش ادمین/کاربر،
  ایموجی پریمیوم، نمایندگی‌ها، گزارش‌ها، KYC، بازیابی تک‌اکانتی، قفل سشن و
  تونل WARP حفظ شده‌اند. هیچ ماژول/تابع عمومیِ نسخهٔ ۲.۳.۱۲ حذف نشده است.
  [فهرست و تطبیق امکانات](docs/full-version.fa.md).
- **ویس و سشن:** peer مخصوص هر اکانت نگهداری می‌شود؛ قطع مدیا، نبود اکانت در
  فهرست شرکت‌کنندگان و ابطال MTProto یکی محسوب نمی‌شوند. خطای موقتِ موتور
  هرگز دلیل بازسازی سراسری یا بیرون‌انداختن سفارشِ دیگر نیست؛ پایان یک سفارش
  اتصال سفارش‌های دیگر روی همان اکانت/چت را نگه می‌دارد. وضعیت رسانه در لاگ
  از شمارندهٔ حضور پایدار جداست؛ DNS/UDP-53 آزمایش مدیای تلگرام نیست.
- **سیاست سفارش:** ظرفیت کمتر، سفارش ۵۰تایی را متوقف نمی‌کند و قیمت را به
  نسبت اکانت‌ها کم نمی‌کند؛ مبنا قیمت کامل پلن و مدت واقعی از `started_at` است.
  بدون حتی یک اکانت واجدشرایط از ابتدا خرید انجام نمی‌شود؛ اگر هیچ joinی موفق
  نشود، مسیر شکست با عودت اتمیک و یک‌بارمصرف تسویه می‌شود.
- **کیف پول و زمان‌بندی:** خرید/کسر موجودی/ثبت تراکنش، عودت لغو و شارژ
  تأییدشدهٔ درگاه هر کدام اتمیک هستند. کلیک تکراری پول دوباره کسر نمی‌کند؛
  رقابت لغو با جاب سفارش زمان‌بندی‌شده دیگر سفارش تسویه‌شده را دوباره اجرا
  یا به‌عنوان «شروع شده» اعلام نمی‌کند. ساخت فاکتور یا کسرِ تأییدنشده ادعا
  نمی‌شود؛ در صورت شکست شروع executor، شناسهٔ سفارش به خریدار اعلام می‌شود.
- **ایمنی اجرا:** انتشار پورت PostgreSQL و Redis در Compose فقط روی
  `127.0.0.1` است. ابزار خلاصهٔ رخدادهای تماس بدون نمایش سشن اضافه شد.

**محدودیت مهم:** این نسخه در محیط آفلاین آزموده شده؛ سلامت سشن‌های واقعی،
رسانهٔ UDP و وضعیت سفارش ۸۴۶ از راه دور تأیید نشده است. در `main` ادغام و روی
سرور مستقر نشده؛ قبل از استقرار، [هشدار سفارش فعال](docs/session-incident.md)
و برنامهٔ مهاجرت را بخوانید.

</div>

---

## نسخهٔ ۲.۳.۱۲ — 🔬 تشخیص دقیق‌تر علت «۱ فعال از ۲۶» و جلوگیری از غیرفعال‌سازی اشتباه

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱

- با آزمون رگرسیون ثابت شد `FloodWait:401` (۴۰۱ **ثانیه** انتظار) در مسیر
  سفارش اشتباهاً با خطای احراز هویت RPC ۴۰۱ یکی گرفته و سشن را `dead` می‌کرد.
  طبقه‌بندی خطا در سفارش، موتور ویس، بررسی سلامت و پروبِ بازیابی اصلاح شد.
- غیرفعال‌سازی فقط برای خطای صریح احراز هویت و به شکل اتمی برای **همان
  ciphertext ذخیره‌شده هنگام خطا** انجام می‌شود؛ کلید تازه‌ای که هم‌زمان
  دوباره لاگین شده غیرفعال نمی‌شود. در دیتابیس برچسب ثابتِ بدون متنِ خطا
  ثبت می‌شود؛ ورود جدید هم پرچم `dead` قدیمی را به `unknown` برمی‌گرداند.
- کپی Session String به نمایندگی (حتی callbackهای قدیمی) بسته شد. ابزار
  `tools/session_audit.py` فقط‌خواندنی و بدون اتصال به Telegram است: آمار
  وضعیت و دلیل تاریخی، قابلیت رمزگشایی و کلیدهای مشترک بین ردیف‌ها را
  بدون افشای شماره، سشن یا اثر انگشت در خروجی خلاصه می‌کند.
- هیچ ردیف قدیمی به‌صورت گروهی فعال نمی‌شود. **علت تاریخیِ هر یک از ۲۵
  ردیف و اعتبار فعلی کلیدها تا دریافت خروجی سرور/پروب امنِ تک‌اکانتی
  نامعلوم است.** [راهنمای ممیزی و بازیابی](docs/session-safety.fa.md).

</div>

---

## نسخهٔ ۲.۳.۱۱ — 🧪 بازیابی امن و تک‌اکانتیِ وضعیت‌های تاریخی «غیرفعال»

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱

- وضعیت ثبت‌شدهٔ ۲۵ اکانت `inactive/dead` به معنی اثبات ابطال ۲۵ سشن نیست؛
  نسخه‌های قدیمی گاهی ۴۰۶ را اشتباهاً به عنوان «سشن مرده» ذخیره می‌کردند.
  منوی سلامت اکنون این عدم قطعیت را روشن می‌کند و دکمهٔ بازیابی هر اکانت را
  جداگانه، با هشدار و تأیید دو مرحله‌ای، در کارت همان اکانت می‌گذارد.
- فقط `get_me()` زنده **و سپس قطع تأییدشدهٔ اتصال** اجازهٔ فعال‌سازی می‌دهند.
  حالت‌های ۴۰۶، ابطال صریح ۴۰۱، تایم‌اوت، مشغول‌بودن کلید و قطع نامطمئن
  هیچ تغییری در دیتابیس ایجاد نمی‌کنند. بازیابی داخل پردازهٔ اصلی و با محافظ
  مالکیت auth key اجرا می‌شود، نه از اسکریپت یا پردازهٔ جداگانه.
- UPDATE دیتابیس به شناسه، ربات، وضعیت غیرفعال و **عین سشنِ تست‌شده** محدود
  است تا تغییر هم‌زمان سشن باعث فعال‌شدن کلید تست‌نشده نشود. پرچم تاریخی
  `spam_status=dead` به `unknown` تبدیل می‌شود، نه `clean`؛ وضعیت اسپم از
  `get_me()` قابل استنتاج نیست و دلیل قدیمی برای بررسی بعدی حفظ می‌شود.
- دکمه‌ها و callbackهای قدیمیِ سینک و حذف **انبوه** اکانت‌های غیرفعال تا
  روشن‌شدن اعتبار سشن‌ها غیرفعال هستند؛ حذف تک‌اکانتی باقی است. تست‌های
  آفلاین مسیر تأیید، قطع اتصال، قرنطینه و شرط دیتابیس را پوشش می‌دهند.

[راهنمای بازیابی](docs/session-safety.fa.md) — صحت سشن‌های واقعی فقط روی سرور
با بررسی کنترل‌شدهٔ تک‌اکانتی مشخص می‌شود؛ این نسخه کلیدِ واقعاً باطل‌شده را
احیا نمی‌کند و جلوی برنامهٔ شخص ثالثِ خارج از محافظ ربات را نمی‌گیرد.

</div>

---

## نسخهٔ ۲.۳.۱۰ — 🔒 جلوگیری از تداخل کلید سشن در همان یک پردازه

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱

- شناسایی یک ریشهٔ مستقل از «اجرای دو ربات»: کپی سشن به ردیف‌های نمایندگی با
  `account_id` متفاوت و **یک auth key مشترک**، حتی در یک پردازهٔ واحد می‌توانست
  دو اتصال هم‌زمان بسازد. رزرو اکنون بر اساس اثرانگشت auth key است؛ فقط پس از
  قطع تأییدشده آزاد می‌شود و TTL دیگر اتصال زنده را آزاد نمی‌کند.
- اشکال ورود با شماره: کلاینتِ فقط-`connect()` با `stop()` قطع نمی‌شد، اما همان
  کلید در دیتابیس ذخیره می‌شد. اکنون `disconnect()` قطعی پیش از ذخیره الزامی
  است؛ import سشن و مسیرهای warmup/join/cancel/shutdown هم از cleanup امن و
  قرنطینهٔ قطع نامطمئن استفاده می‌کنند.
- قفل تک‌نمونه‌ای فایل از مسیر ثابت پروژه استفاده می‌کند؛ قفل advisory
  PostgreSQL نیز پیش از اتصال به اکانت‌ها، بین نمونه‌های **نسخهٔ جدید** با
  دیتابیس مشترک برقرار می‌شود. از دست‌رفتن قفل، فرایند را fail-closed می‌بندد.
- تشخیص ۴۰۶ فقط برای `AUTH_KEY_DUPLICATED` است، نه هر کد ۴۰۶. برای حفظ استخر
  اکانت‌ها غیرفعال‌سازی حدسی انجام نمی‌شود، اما ۴۰۶ هیچ تضمینی دربارهٔ
  **معتبرماندن** کلید نمی‌دهد؛ کلید باطل‌شده نیازمند ورود دوباره است.
- تست‌های آفلاین برای هم‌کلیدیِ کپی نمایندگی، قطع/لغو اتصال، قفل دیتابیس و
  جداسازی ۴۰۶ از ابطال صریح اضافه شدند.

**اصلاح برداشت نسخهٔ ۲.۳.۹:** در توضیح تاریخیِ آن نسخه، «تمام کلیدها سالم‌اند»
و «حتماً سرور دیگری وصل است» نتیجه‌گیری قطعی از گزارش ۴۰۶ بود؛ آن گزارش
چنین چیزی را اثبات نمی‌کرد. همچنین لاگ راه‌اندازیِ سرور به‌تنهایی برای نسبت
دادن خطا به یک ریشهٔ مشخص کافی نیست. [راهنمای استقرار و بازیابی](docs/session-safety.fa.md).

</div>

---

## نسخهٔ ۲.۳.۹ — 🛡 پایان «غیرفعال‌شدن خودکار»: 406 دیگر اکانت را نمی‌سوزاند

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱

**تشخیص ماجرا (گزارش سینک: ۲۵/۲۵ «فعال در مکان دیگر (406)» و ۰ نیازمند
لاگین مجدد):** همهٔ کلیدها **سالم‌اند** (صفرِ relogin_required ثابتش کرد) —
مشکل این بود که هم‌زمان یک اتصالِ زندهٔ دیگر جایی با همان کلیدها کار می‌کرد؛
ولی منطق قدیمی 406 را «کلید باطل» می‌دانست و اکانت را فوراً **inactive**
می‌کرد. نتیجهٔ غلط: استخر ۲۵تایی همه «غیرفعال» شد و سفارش‌ها فقط ۱ اکانت
می‌دیدند. AUTH_KEY_DUPLICATED یعنی «کسی الآن با این کلید وصل است»، نه «کلید
مرده».

**قانون جدید (حالتی که کاربر خواست):** اکانت **فقط** با مرگ واقعیِ کلید
(`SESSION_REVOKED` / `AUTH_KEY_UNREGISTERED` / `AUTH_KEY_INVALID` /
`USER_DEACTIVATED` / 401 / Unauthorized — نسخه‌های CamelCase Kurigram هم
پوشیده) غیرفعال می‌شود. هیچ مسیر دیگری — نه 406، نه شبکه کند، نه کرش —
حق غیرفعال‌کردن خودکار ندارد.

**سه محل که اصلاح شدند:**
1. `voice_call_manager._mark_session_dead` — دیسپچر شد: قبل از inactive
   نشانهٔ مرگ واقعی لازم است؛ در غیر این صورت فقط یادداشت نرم «cooldown» +
   لاگ هشدارِ راهنما (نمونهٔ بیگانه/سرور قدیمی باید خاموش شود).
2. `order_executor` — هم در تلاشِ تکی، هم در موج: 406 اکانت را فقط برای
   **همان سفارش** کنار می‌گذارد (هشدار «transient, NOT disabled») و شمارندهٔ
   ۵-متوالیِ خودایستِ سیلاب 406 را حفظ می‌کند؛ دیگر `_mark_account_dead`
   صدا نمی‌زند.
3. `health_checker` — بلوکِ غیرقابل‌دسترسیِ مرده (بعد از return) بازنویسی
   شد؛ نتیجهٔ موفق دوباره ذخیره می‌شود؛ غیرفعال‌سازی فقط با مرگ واقعی و هرگز
   نه با 406.

**سینک مجدد هم محافظتی‌تر شد:** پس از ۵ پروبِ متوالیِ 406، پروب‌های باقی‌مانده
متوقف می‌شوند (جنگیدن با کلیدِ در‌حال‌استفاده هر دو طرف را می‌سوزاند) و
گزارش، راهنمای دقیق «نمونهٔ بیگانه کجاست» (سرور قدیمی روشن / stray / فروشندهٔ
سشن چندفروش) و راه‌حل قطعی «لاگین مجدد = کلید تازه» را می‌دهد.

**تست‌های رگرسیون:** `NeverAutoDisableOn406Tests` — گیت fatal در vcm،
health-checker فقط-fatal، wave-branch 406 بدون dead-mark، توقف محافظتی resync.

</div>

---

## نسخهٔ ۲.۳.۸ — 🚇 تونل کامل WARP برای صدا: مدیا/UDP ویس‌کال هم از WARP می‌گذرد

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱

**ریشهٔ «ghost: media connection missing»:** ایمیج `caomingjun/warp` به‌حالت
پیش‌فرض **فقط پروکسی SOCKS5** می‌سازد و تونل لایهٔ ۳ (TUN) برقرار نمی‌کند.
سیگنالینگ MTProtoِ کلاینت‌ها چون پروکسی SOCKS5 ست داشت از WARP رد می‌شد
(برای همین join می‌خورد)، اما **موتور مدیا ntgcalls هیچ پروکسی‌ای نمی‌فهمد**
و بسته‌های UDP/WebRTC ویس‌کال **مستقیم از IP هاست** خارج می‌شدند — یعنی مسیر
فیلترشده → مدیا هرگز وصل نمی‌شد → اکانت «روح» (حاضر بی‌صدا) می‌ماند و
تلگرام در تلاش‌های rejoin پیاپی `GROUPCALL_ADD_PARTICIPANTS_FAILED` می‌داد.

**راه‌حل:** گذر به **NAT mode** رسمی warp-docker (`WARP_ENABLE_NAT=1`):
کلاینت با `warp-cli mode warp` اینترفیس `CloudflareWARP` و default route را
می‌سازد و چون ربات در همان فضای شبکه اجراست، **کل ترافیکش — از جمله UDP
ویس‌کال — از تونل WARP عبور می‌کند.** sysctlهای `ip_forward`/IPv6 طبق سند
رسمی اضافه شد.

**سپر تشخیصی:**
- healthcheck سرویس warp حالا علاوه‌بر `warp=on` **وجود اینترفیس TUN** را هم
  اجباری می‌کند؛ پس اگر روزی تونل نیاید، ربات اصلاً استارت نمی‌شود (به‌جای
  کارِ ظاهراً-سالم با مدیای شکسته).
- `deploy-warp.sh` حالا هم «خروج مستقیم بدون پروکسی» را باید `warp=on` ببیند
  (همان مسیری که UDP می‌رود) و هم وجود TUN را گزارش/هشدار می‌دهد.

**تست‌های رگرسیون:** `WarpFullTunnelTests` — وجود `WARP_ENABLE_NAT=1` و
`ip_forward`، اجبار TUN در healthcheck، و network_mode مشترک bot با warp.

**نکتهٔ دیپلوی:** تغییر env/healthcheck است؛ یک‌بار `docker compose down`
و سپس `docker compose up -d --build` (یا `bash deploy-warp.sh`) لازم است.

</div>

---

## نسخهٔ ۲.۳.۷ — 🔒 قفل تک‌نمونه‌ای: دیگر دو کپیٔ ربات هم‌زمان اجرا نمی‌شوند

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱

**یافتهٔ نهایی ماجرا:** خروجی `ps aux` سرور نشان داد پروسسِ
`root 733783 python main.py` (از ساعت ۱۹:۴۵) **مستقیم روی هاست، کنار
کانتینر داکر** در حال اجراست — دو نسخهٔ هم‌زمانِ ربات با سشن‌های مشترک =
پی‌نگ‌پانگ `AUTH_KEY_DUPLICATED` و ابطال پیاپی کلیدها (سفارش‌های ۸۳۴).
پس از کشته‌شدن نمونهٔ اضافی، اکانت سالم بدون مشکل join شد (سفارش ۸۳۶).

**راه‌حل دائمی در کد:** قفل OS-level (flock) روی
`data/.bot_instance.lock` — همان فایلی که به‌خاطر volume `.:/app` هم هاست
و هم کانتینر آن را می‌بینند:

- پروسسِ دوم (هر جایی: هاست، داکر، اسکریپت دیگر) به جای وصل‌شدن و سوزاندن
  سشن‌ها، با **هشدار بلند هر ۳۰ ثانیه در لاگ** صبر می‌کند تا نمونهٔ قبلی
  آزاد شود — و خودش به‌خودی‌خود دوباره قفل را می‌گیرد و ادامه می‌دهد.
- قفل با مرگ پروسس خودکار آزاد می‌شود؛ بعد از kill/reboot قفل بیایهٔ
  گیرکرده وجود ندارد.
- قبل از هر اتصال/دیتابیس اعمال می‌شود؛ روی پلتفرم بدون flock (ویندوز)
  fail-open است تا توسعه نشکند.

**یادآوری عملیاتی:** اگر در لاگ `🔒 یک نمونهٔ دیگر از ربات همین حالا در
حال اجراست` دیدی، یعنی یک کپیٔ دوم بالاست — با
`ps aux | grep "python main.py"` پیدایش و بکشش.

</div>

---

## نسخهٔ ۲.۳.۶ — 🕵️ تشخیص «سشن در مکان دیگر فعال است» + توقف خودکار موج سوختن

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۷/۰۱

**یافتهٔ میدانی (لاگ سفارش ۸۳۴):** ۱۵ اکانت از ۱۶، همگی **در اولین بستهٔ
handshake (`InvokeWithLayer`) ظرف کسری از ثانیه** با 406 رد شدند، ولی
اکانت ۱۱۲ سالم وصل و join شد. یعنی دیگر حرف از سیلاب اتصال/ریس نیست:
**این سشن‌ها همین حالا در یک پروسس/سرور/پنل دیگر «زنده و آنلاین» نگه
داشته می‌شوند** (اتصال نیمه‌مردهٔ crash حداکثر چند دقیقه دوام دارد؛ نه
ساعت‌ها). تا آن نمونهٔ دیگر قطع نشود، پی‌نگ‌پانگ ابطالِ کلید ادامه دارد.

**این نسخه سه ابزار اضافه می‌کند:**

1. **`fetch_me_status()`** — تست سشن حالا دلیل شکست را دسته‌بندی می‌کند:
   ``duplicated_in_use`` (۴۰۶: جای دیگر فعال) / ``relogin_required`` /
   ``timeout`` / ``error``. خطای `SessionInUseError` (مشغول در ویس‌کال)
   هم دیگر بلع نمی‌شود و به‌درستی «مشغول» گزارش می‌شود (در ۲.۳.۲ اشتباهاً
   «مرده» حساب می‌شد).
2. **گزارش «سینک مجدد سشن‌ها» دسته‌بندی شد** و حالا بخش جداگانه‌ای دارد:
   «⚠️ N اکانت سشن‌شان از جای دیگری آنلاین است — آن نمونه را پیدا و خاموش
   کنید، یا لاگین مجدد بزنید تا کلید تازه ساخته و نسخهٔ دیگر نابود شود»
   + بخش‌های واقعاً-باطل (relogin) و تایم‌اوت شبکه.
3. **توقف خودکار موج ساخت پس از ۵ پیاپیِ 406 متوالی**
   (`AUTH_KEY_DUPLICATED_SYSTEMIC`): وقتی الگو سیستماتیک است (همه به یک
   خطا می‌میرند)، بقیهٔ موج‌ها زده نمی‌شود تا بقیهٔ اکانت‌های سالم هم در
   آتش ابطال نسوزند؛ در لاگ، علت و راه‌حل دقیق نوشته می‌شود.

**اقدام لازم از سمت شما (عملیاتی):** آن نمونهٔ دیگر را پیدا کنید —
`docker ps -a` (کانتینر روباتِ پروژهٔ قدیمی)، `ps aux | grep main.py`
(پروسس دستی روی هاست)، سرور قدیمی هنوز روشن، یا فروشنده/پنل دیگری که روی
همان سشن‌ها کار می‌کند. سپس روی اکانت‌های آلوده «لاگین مجدد» بزنید: لاگین
تازه کلید جدید می‌سازد و نسخهٔ دیگر را خودکار نابود می‌کند.

</div>

---

## نسخهٔ ۲.۳.۵ — 🔌 پایان چرخهٔ «چندبار از سشن خارج شدن» (AUTH_KEY_DUPLICATED)

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۳۱

**علائم:** سیلان `406 AUTH_KEY_DUPLICATED` برای ده‌ها اکانت هم‌زمان، سپس
خارج‌شدن پیاپی اکانت‌ها از سشن، و در نهایت این‌که «اکانت‌ها وارد ویس‌کال
نمی‌شدند».

**مکانیزم خرابی:** کلید سشن هم‌زمان در دو اتصال فعال استفاده می‌شد (مثلاً
اتصال‌های نیمه‌بازِ پروسس/کانتینر قبلی + اتصال‌های تازهٔ resume بعد از
ری‌استارت/کرش، به‌خصوص وقتی ده‌ها کلاینت در یک ثانیه ساخته می‌شدند) →
تلگرام کلید را **برای همیشه** باطل می‌کرد → چون 406 در هیچ‌کدام از لیست‌های
«مرگبار» نبود، سیستم دوباره و دوباره همان کلید مرده را retry می‌کرد
(پدیدهٔ «چند‌بار خارج شدن») → در عمل کل پول اکانت خالی و join ناممکن می‌شد.

**فیکس‌ها (دفاع چندلایه):**

1. **406 مرگبار اعلام شد، همه‌جا:** در مسیر ساخت/warmup کلاینت ویس
   (`_mark_session_dead` جدید)، در `start_call`، در هر دو لیست مرگبارِ
   order_executor، در طبقه‌بندی `join_brain` و در تشخیص مردهٔ
   `health_checker` — اکانت یک‌بار `inactive/dead` علامت می‌خورد و retry
   می‌ایستد.
2. **ساخت کلاینت‌ها pacing گرفت:** سقف همزمانی `CLIENT_CREATE_CONCURRENCY`
   از ۸ به **۲** کاهش یافت (قابل تنظیم از env) و بعد از هر `client.start()`
   مکث تصادفی ۰٫۸–۲ ثانیه گذاشته شد تا resume بعد از ری‌استارت دیگر به
   الگوی سیلابی تبدیل نشود.
3. **لیک رزروی سشن دیگر ممکن نیست (علت «وارد ویس‌کال نشدن»):** رزروهای
   ad-hoc حالا **TTL دارند** (`SESSION_ADHOC_TTL_SEC`، پیش‌فرض ۵ دقیقه) و
   چیزی که وسط راه لغو شده باشد (مثل wait_for) خودبه‌خود منقضی می‌شود؛
   `acquire_voice` هم دیگر بی‌نهایت صبر نمی‌کند (`VOICE_ACQUIRE_WAIT_SEC`،
   پیش‌فرض ۹۰ ثانیه) بلکه خطای کنترل‌شده می‌دهد.
4. **`fetch_me` مقاوم به لغو شد** (همان متدی که «سینک مجدد سشن‌ها» استفاده
   می‌کند): اتصال سبکِ `connect` صریح + قطعِ `shield`شده + آزادسازی رزرو در
   `finally` — دیگر نه زامبی‌اتصال داریم نه اکانتِ گیرکرده «مشغول».
5. **رفع دابل‌استارت دیسپچر** در `check_spambot` (`async with` خودش start
   می‌کند؛ فراخوانی دوم حذف شد).

**نکتهٔ بهره‌برداری:** اکانت‌هایی که کلیدشان با 406 باطل شده، واقعاً سوخته‌اند
و باید در پنل حذف و دوباره لاگین شوند؛ دکمهٔ «🔄 سینک مجدد سشن‌ها» فقط
سشن‌هایی را برمی‌گرداند که هنوز معتبرند.

</div>

---

## نسخهٔ ۲.۳.۴ — ✅ فیکس قطعی دکمهٔ لغو: «در PTB>=v20 هر گروه فقط یک هندلر»

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۳۰

**علامت:** حتی روی ۲.۳.۳ دکمهٔ «🛑 لغو سفارش» هیچ واکنشی نداشت.

**ریشهٔ واقعی و نهایی (اثبات‌شده با خودِ کتابخانهٔ نصب‌شده روی سرور،
python-telegram-bot 22.8):** در PTB نسخهٔ 20 به بعد معنای `block` عوض شده؛
در کد `Application.process_update` بعد از اجرای اولین هندلرِ مچ‌شده در هر
گروه، `break  # Only a max of 1 handler per group is handled` اجرا
می‌شود — یعنی در هر گروه **دقیقاً یک هندلر** اجرا می‌شود و `block=False`
فقط کالبک را به‌صورت تسک هم‌زمان زمان‌بندی می‌کند، نه اینکه آپدیت را به
هندلر بعدیِ همان گروه بسپارد. پس:

- نسخهٔ ۲.۳.۲ (جابه‌جایی ترتیب در group=-1): بی‌اثر بود چون گارد ضداسپم —
  `TypeHandler(Update)` که با **هر** آپدیتی مچ می‌شود — اولِ همان گروه بود
  و همیشه تنها اجراشونده می‌شد.
- نسخهٔ ۲.۳.۳ (`block=False`): بر پایهٔ برداشت اشتباه از معنای `block`
  بنا شده بود و در عمل هیچ تغییری ایجاد نکرد (با شبیه‌ساز واقعی کتابخانه
  اثبات شد خروجی آن `[]` است — هندلر لغو هرگز اجرا نمی‌شود).

**فیکس ۲.۳.۴ — هر مرحلهٔ سراسری، گروه مستقل:**

| گروه | مرحله |
|---|---|
| `-4` | هندلر `cancel_order_<id>` — **اولین چیزی که اصلاً پردازش می‌شود**؛ در همهٔ حالت‌ها (حتی کاربر میوت‌شده/حالت تعمیرات) جواب می‌دهد چون لغو سفارشِ خودِ کاربر امن و idempotent است |
| `-3` | گارد ضداسپم (`TypeHandler`) — تنها هندلر گروهش؛ میوت با `ApplicationHandlerStop` awaited اعمال می‌شود |
| `-2` | نگهبان تعمیرات (۳ الگوی متقابلاً انحصاری) |
| `-1` | پیش‌روتر منوها |

اثبات با همان نسخهٔ 22.8: چیدمان جدید خروجی
`['CANCEL_FIRED', 'guard']` می‌دهد و حتی برای کاربر میوت‌شده
`['CANCEL_FIRED', 'guard_raise']` — لغو همیشه اجرا می‌شود.

**به‌علاوه:**
- لاگ «cancel_order_callback fired: user=… order_id=…» برای اثبات اجرای
  هندلر در لاگ سرور (INFO).
- ثبت تکراری هندلر لغو حذف شد (خطر اجرای دوباره/پیام دوم).
- تست‌های رگرسیون سطح-سورس اضافه شد تا این الگوی شکسته هرگز برنگردد
  (📄 `tests/test_anti_spam.py::HandlerGroupRoutingTests`).

</div>

---

## نسخهٔ ۲.۳.۳ — 🔧 ریشه‌یابی نهایی دکمهٔ لغو: کل group=-1 بلعیده می‌شد

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۳۰

**علامت:** حتی پس از دیپلوی ۲.۳.۲، دکمهٔ «🛑 لغو سفارش» زیر پیام موفقیت
سفارش هیچ واکنشی نداشت.

**ریشهٔ واقعی (عمیق‌تر از حدسی ایموجی پریمیوم — که سالم بود و فقط هشدار
زیباشناختی می‌داد):** `_spam_guard` به‌صورت `TypeHandler(Update)` در همان
گروه `-1` ثبت شده بود و **با هر آپدیتی مچ می‌شد**؛ چون `block` پیش‌فرض
(=True) داشت، پردازش گروه `-1` همیشه همین‌جا تمام می‌شد و هیچ هندلر بعدی
آن گروه — از جمله هندلر سراسری `cancel_order_<id>`، نگهبان تعمیرات و حتی
«پیش‌روتر منوها» — هرگز اجرا نمی‌شد. به همین دلیل جابه‌جایی ترتیب ثبت در
۲.۳.۲ اثری نداشت: هندلر لغو هرگز به نوبت نمی‌رسید.

**فیکس:**
- `TypeHandler(Update, _spam_guard, block=False)` و هر سه ثبت
  `_maintenance_guard` با `block=False` — گاردها فقط «عبوری»‌اند و آپدیت را
  به هندلرهای بعدی گروه می‌سپارند.
- مسدودسازیِ واقعی دست نخورده می‌ماند: کاربر میوت‌شده در اسپم‌گارد و کاربر
  غیرسوپر در حالت تعمیرات همچنان با `raise ApplicationHandlerStop` سخت
  متوقف می‌شوند.
- اثر جانبی مثبت: «پیش‌روتر منوها» (رفع ریشه‌ای «دکمه‌ها جواب نمی‌دهند»)
  که صامت مرده بود، حالا واقعاً فعال است.
- لاگ دیباگ «cancel pressed» از هندلر لغو حذف شد.

</div>

---

## نسخهٔ ۲.۳.۲ — 🔧 فیکس دکمه لغو سفارش + «سینک مجدد اکانت‌های سوخته»

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۳۰

### ۱) دکمه «لغو سفارش» کاربر دوباره پاسخ می‌دهد ❌
**باگ:** نگهبان «حالت تعمیرات» یک `CallbackQueryHandler` بدون فیلتر در
گروه `-1` بود و اولِ ثبت می‌شد؛ چون PTB در هر گروه فقط *اولین* هندلرِ
تطبیق‌یافته را اجرا می‌کند، هندلر سراسری `cancel_order_<id>` که بعد از آن
ثبت شده بود هرگز اجرا نمی‌شد و دکمهٔ «❌ لغو سفارش» در «📦 سفارشات من» /
روی پیام نتیجهٔ خرید کاملاً **بی‌پاسخ** بود.

**فیکس:** ترتیب ثبت جا‌به‌جا شد — هندلر لغو حالا **قبل** از نگهبان تعمیرات
ثبت می‌شود، پس همیشه و آنی پاسخ می‌دهد (لغو زمان‌بندی‌ = عودت کامل،
لغو در-حال-اجرا = محاسبهٔ مصرف-ثانیه‌ای و عودت مانده). لاگ دیباگِ موقتِ
نگهبان هم پاک شد.

### ۲) «🔄 سینک مجدد سشن‌ها» — بازگردانی اکانت‌های ظاهراً سوخته 🔌
**مشکل:** اکانت‌هایی که سشنشان سالم است اما به‌خاطر نوسانِ شبکه/WARP،
رقابتِ موقتِ AUTH_KEY_DUPLICATED هنگام ری‌استارت کانتینر، یا خطای گذرای
تلگرام غلط طبقه‌بندی «inactive» شده بودند، دیگر وارد پول سفارش نمی‌شدند
(نمونهٔ گزارش‌شده: `Requested=50, Eligible=9`) و تنها کنترل، حذف‌شان بود.

**راه‌حل («🚑 گزارش سلامت اکانت‌ها» → دکمهٔ جدید):**
- «🔄 **سینک مجدد سشن‌ها (بازگردانی خودکار)**» روی گزارش سلامت و لیست
  سوخته‌ها: برای هر اکانت غیرفعال یک اتصال سبکِ واقعی (start‌+get_me) می‌زند؛
  اگر سشن جواب بدهد، وضعیت بلافاصله به `active` برمی‌گردد و پرچم اسپمی
  `dead` پاک می‌شود — اکانت دیگر از همان لحظه در پول سفارش‌ها فعال‌اند.
- اکانتِ مشغولِ ویس‌کال (SessionInUse) بی‌خطر رد می‌شود (بدون خطرِ
  AUTH_KEY_DUPLICATED دوباره) و دست‌نخورده می‌ماند؛ سشن واقعاً مرده
  (SESSION_REVOKED/AUTH_KEY_INVALID) در همان وضعیت می‌ماند تا سشن دوباره ساخته شود.
- گزارش پایانی: چه تعداد بازگردانده شد، چه تعداد هنوز مرده، چه تعداد در کال.

### ۳) تمیزکاری‌های موازی
- پترن‌های مردهی ظرفیت (`cap_retry` / `cap_slot_*`) از main.py پاک شدند —
  به تبعِ حذف کامل گارد ظرفیت در ۲.۳.۱.
- `BOT_VERSION = "2.3.2"`.

</div>

---

## نسخهٔ ۲.۳.۱ — 🧹 حذف کامل گارد ظرفیت + رندر صحیح متغیرهای متن استارت

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۹

### ۱) حذف کامل گارد ظرفیت از پروژه 🧹
به درخواست مشتری، «اوج مصرف / تکمیل ظرفیت / پیشنهاد ساعت آزاد» از **تمام**
پروژه برداشته شد — نه در پیام کاربر، نه در منطق پذیرش سفارش:

- پیش‌نمایش ظرفیت از کادر «🧾 تایید نهایی سفارش» حذف شد — متن دوباره دقیقاً
  مثل قبل است: سرویس، تعداد، لینک، زمان اجرا، مبلغ، سؤال تأیید.
- پرداخت نهایی (`confirm_order_pay`) دیگر هیچ چک ظرفیتی ندارد؛ سفارش مستقیم
  به بررسی تداخل زمانی + اعتبار کیف‌پول می‌رود (رفتار نسخه‌های قبل از ۲.۲.۲).
- callbackهای `cap_retry` / `cap_slot_*` (رزرو ساعت پیشنهادی/بررسی مجدد)
  کاملاً حذف شدند.
- `services/capacity_planner.py` و `tests/test_capacity_planner.py` حذف شدند؛
  `DatabaseManager.get_capacity_reservations` هم پاک شد.
- سکشن `CAPACITY_*` از `.env.example` حذف شد.

> ✅ «قفل لینک هوشمند» (جلوگیری از تداخل زمانیِ دو سفارش روی **یک لینک**)
> دست‌نخورده باقی مانده — آن مکانیک درونیِ سفارش است و ربطی به محدودیت
> تعداد اکانت ندارد.

### ۲) رندر صحیح متغیرهای متن استارت ✅

**باگ:** رندر متن استارت فقط `{name}` `{id}` `{username}` `{credit}` را می‌شناخت؛
متن سفارشی ادمین که `{brand}` `{tagline}` `{intro}` `{services}` `{benefits}`
`{guide}` `{support}` `{support_hours}` `{cta}` داشت، **خام** نمایش داده می‌شد.

**فیکس (`handlers/general_handlers.py`):**
- رندر دیگر با `str.format` نیست (جایگذاریِ REPLACE دستی) تا هیچ متغیر
  ناشناخته‌ای رندر را نشکند و هیچ placeholder خامی در پیام کاربر دیده نشود.
- همهٔ متغیرها پیش‌فرض فارسیِ معنادار گرفتند: `{brand}` (نام نمایندگی یا
  bot_username@)، `{tagline}`، `{intro}`، `{services}`، `{benefits}`،
  `{guide}`، `{support}`، `{support_hours}`، `{cta}` + `{first_name}`
  و `{user_id}` علاوه بر متغیرهای قبلی.
- هر متغیر با کلید `start_<var>` در bot_settings قابل بازنویسی است
  (بدون ری‌استارت).
- `BOT_VERSION = "2.3.1"`.

</div>

---

## نسخهٔ ۲.۳.۰ — 🛡 حالت ضد اسپم: خروج یک‌هفته‌ایِ دونه‌به‌دونه + کول‌داون لغو

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۹

### ۱) حالت «ضد اسپم و محافظت از اکانت‌ها» (پنل جدید سوپرادمین) 🛡

**مشکل:** الگوهای ماشینی ربات (burst ورود/خروج غیرانسانیِ ده‌ها اکانت هم‌IP،
رناتِ بی‌استراحت، آهنگ کاملاً پریودیک) اصلی‌ترین دلیل حذف/محدود شدن اکانت‌ها
توسط آنتی‌اسپم تلگرام بودند.

**راه‌حل (`services/anti_spam.py`):** تنظیمات در دیتابیس ذخیره و از منوی
«🛡 ضد اسپم و محافظت» در تنظیمات سیستم **بدون ری‌استارت** اعمال می‌شوند
(env فقط fallback اولیه است):

- **تاگل کلی ضد اسپم:** وقتی فعال است، موج join با فاصلهٔ ۳–۸ ثانیه + jitter
  انسانی و سقف موج محافظه‌کار (پیش‌فرض ۲) شلیک می‌شود و خروج از ویس‌کال هم
  پراکنده‌تر (۱.۵–۴ ثانیه + jitter) است. وقتی خاموش است، دقیقاً رفتار قبلی
  (مقادیر `VOICE_*` فعلی) حفظ می‌شود — صفر-breaking-change.
- **استراحت اکانت بین سفارش‌ها (😴):** اکانتی که کارش را تازه تمام کرده تا
  X دقیقه (پیش‌فرض خاموش) برای سفارش بعدی انتخاب نمی‌شود؛ تایمر هر اکانت با
  ±۲۰٪ jitter روی `data/account_rest.json` پایدار می‌شود (ری‌استارت-اثبات) و
  در انتخاب کاندید/زمان‌بندی موج/ردیابی خطا و refill لحاظ می‌گردد؛ اگر همهٔ
  پول در استراحت باشد، موتور موج تا نزدیک‌ترین پایان استراحت صبر کرده و مجدد
  اسکن می‌کند (با سقف، نه بی‌نهایت).
- همهٔ مسیرها fail-open هستند: خطای دیتابیس/کش هیچ‌وقت join یا leave را
  بلاک نمی‌کند.

### ۲) خروج گروه/کانال دیگر فوری و یک‌جا نیست — یک هفته بعد، دونه‌به‌دونه 🚪

**مشکل:** در لحظهٔ لغو/پایان سفارش، همهٔ اکانت‌ها فوراً و با هم از گروه خارج
می‌شدند (سیگنال کتابی ربات)؛ اگر مشتری دوباره برای همان گروه سفارش می‌زد،
چرخهٔ مضر leave → rejoin ریسک را دوچندان می‌کرد.

**رفتار جدید (`services/group_leave_scheduler.py` + جدول `pending_group_leaves`):**
- در لحظهٔ پایان/لغو سفارش اکانت **فقط از ویس‌کال** خارج می‌شود (paced، مثل قبل)؛
  خروج از خودِ گروه/کانال برای **هر اکانت** جداگانه زمان‌بندی می‌شود:
  پیش‌فرض **۱۶۸ ساعت (یک هفته)** بعد + فاصله‌گذاری صفی (هر اکانت،
  «شمارهٔ نفر در صف همان گروه × فاصلهٔ خروج») → اجرای نهایی دقیقاً
  **به‌ترتیب** و **دونه‌به‌دونه** (پیش‌فرض هر ۶۰ ثانیه یک اکانت)، هرگز انبوه.
- جاب sweeper هر ۶۰ ثانیه سررسیدها را با claim اتمیک (pending→processing)
  اجرا می‌کند؛ با چند ربات/پروسه هم دوباره‌کاری رخ نمی‌دهد.
- **سفارش مجدد همان گروه** → خروج‌های در صفِ آن مقصد خودبه‌خود لغو و اکانت‌ها
  عضو باقی می‌مانند (تطبیق روی نام کاربری/هش دعوت، نه چک‌باکس لینک خام). اگر
  خودِ اکانت هم به همان چت برگردد، خروج در صفِ او لغو می‌شود.
- FloodWait دقیقاً محترم است (رکورد به بعد از پایان مهلت + سکوت موکول می‌شود)،
  اکانت سوخته/حذفی بی‌صدا جمع می‌شود، خطای موقت تا ۴ بار بک‌آف (۵/۱۰/۲۰/۴۰
  دقیقه) ری‌ترای و سپس «failed» ثبت می‌شود؛ خطای «عضو نیست/چت نیست» = done.
- اگر قابلیت خاموش باشد یا زمان‌بندی به هر دلیلی شکست بخورد، مسیر قبلی
  (خروج فوریِ paced) اجرا می‌شود — هیچ اکانتی هرگز در گروه «گیر» نمی‌کند.
- برای مسیر گروه/کانال، آی‌دی عددیِ چت join‌شده همان لحظهٔ ورود ثبت می‌شود تا
  خروج به‌تأخیرافتاده با آی‌دی دقیق انجام شود، نه حدس لینک.

### ۳) کول‌داون لغو: ۲۰ دقیقه ممنوعیتِ سفارش جدید ⏳ (قابل تنظیم از پنل)

- کاربری که سفارشش را لغو می‌کند، تا X دقیقه (پیش‌فرض **۲۰ دقیقه**؛ از پنل
  سوپرادمین قابل تغییر/خاموش با ۰) نمی‌تواند سفارش جدید ثبت کند.
- ددلاین روی ستون جدید `users.cancel_cooldown_until` ذخیره می‌شود (پایدار بعد
  از ری‌استارت)، و **دو جا** چک می‌شود: شروع «🛍 خرید سرویس» و دقیقهٔ
  پرداخت نهایی — تا بای‌پسِ فلو ممکن نباشد. پیام فارسیِ محترمانهٔ باقی‌مانده
  هم به کاربر نمایش داده می‌شود.

### ۴) متفرقه
- `BOT_VERSION = "2.3.0"` و همگام‌سازی نسخه در تست‌های رگرسیون.
- `tests/test_anti_spam.py`: ۱۵ تست جدید (پروفایل و pacing فعال/غیرفعال،
  پایداری فایل استراحت اکانت، jitter استراحت، نام‌کاربری canonical لینک،
  گاردهای سیم‌بندی هوک‌ها در executor/VCM/handler/admin/main).
- `.env.example` با کل درجه‌های جدید و توضیح فارسی به‌روزرسانی شد.
- پنل «🛡 ضد اسپم و محافظت» (فقط سوپرادمین): تاگل‌ها، تنظیم تأخیر خروج (ساعت)،
  فاصلهٔ خروج دونه‌به‌دونه (ثانیه)، کول‌داون لغو (دقیقه)، استراحت اکانت
  (دقیقه)، مشاهدهٔ تعداد خروج‌های در صف و «🧹 لغو کل صف».
- کلیدهای وضعیت جدید: `AWAITING_ANTISPAM_VALUE = 230` در کنفسیگ مکالمات.

</div>

---

## نسخهٔ ۲.۲.۳ — 🧰 فیکس منوها: تعمیرات سراسری، آمار کامل، لغو از پنل ادمین

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۷

### ۱) حالت تعمیرات حالا واقعاً همهٔ ربات‌ها را می‌بندد 🌍
- **باگ:** تاگل تعمیرات فقط `bot_data` همان اپی را که ادمین از طریق آن تاگل
  می‌کرد به‌روز می‌کرد؛ ربات‌های نمایندگی (اپ‌های جدا) پرچم قدیمی (خاموش)
  داشتند و کاربرانشان می‌توانستند سفارش بزنند.
- **فیکس:** تنظیم مرجع در `bot_id=1` ذخیره و پرچم همهٔ اپ‌های فعال همان لحظه
  فلیپ می‌شود؛ `bot_manager` هم در استارتاپ هر ربات، پرچم سراسری را می‌خواند.
  یعنی یک کلید = توقف همهٔ ربات‌ها + پایدار بعد از ری‌استارت.

### ۲) سفارش تازهٔ ثبت‌شده در آمار دیده نمی‌شد 📊
- **باگ:** `get_all_order_stats` فقط `running` و `scheduled` را می‌شمرد؛
  سفارش تازه در وضعیت `pending` (صف) بود و در هیچ آماری ظاهر نمی‌شد.
- **فیکس:** آمار حالا «⏳ در صف اجرا»، «📥 امروز» و «✅ تکمیل‌شده» را هم
  نشان می‌دهد (هم «📉 آمار کل ربات» و هم «📊 گزارش کلی»)؛ حذف پیام
  «در حال جمع‌آوری» هم ضدخطا شد (قبلاً خطای delete کل نمایش آمار را می‌خواباند).
- تاریخچهٔ کاربر هم وضعیت `pending` را درست نمایش می‌دهد («⏳ در صف»).

### ۳) لغو سفارش مستقیماً از لیست‌های ادمین 🛑
- **باگ:** در لیست سفارشات ادمین هیچ دکمه‌ای برای انتخاب و لغو هر سفارش
  نبود؛ تنها راه، فلو غیرشهودی «شمارهٔ ردیف بفرست» بود.
- **فیکس:** زیر «📦 سفارشات اخیر/زمان‌بندی‌شده» و «سفارشات کاربر» در پروفایل،
  برای هر سفارشِ قابل‌لغو دکمهٔ «🛑 لغو سفارش #id» اضافه شد → پیش‌نمایش مالی
  (کل/مصرف‌شده/قابل‌عودت) → لغو با عودت / بدون عودت / انصراف.
- سفارش‌های `pending` هم به فهرست «فعال» لیست‌های ادمین اضافه شدند تا
  قابل انتخاب و لغو باشند.

</div>

---

## نسخهٔ ۲.۲.۲ — 🛡 گارد ظرفیت منابع + بازگردانی گزارش مالی ادمین‌ها

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۷

### ۱) گارد ظرفیت منابع قبل از ثبت سفارش (آنی و زمان‌بندی‌شده)

**مشکل:** اگر در یک بازهٔ زمانی، مجموع سفارش‌های فعال/رزروی از ظرفیت اکانت‌های
سالم عبور می‌کرد، اجرا دچار کمبود منبع می‌شد (join ناموفق، سقوط live و در
بدترین حالت لغو زنجیره‌ای سفارش‌ها) و کاربر تا لحظهٔ اجرا بی‌خبر بود.

**راه‌حل (`services/capacity_planner.py`):**
- قبل از پذیرش هر سفارش، مصرف منابع در **کل بازهٔ اجرا** `[شروع، شروع+مدت]`
  سنجیده می‌شود — نه فقط لحظهٔ شروع. پس اگر ساعت ۱۲:۰۰ شلوغ باشد و کاربر
  ۱۲:۱۰ تلاش کند، سفارشش پذیرفته **نمی‌شود**؛ سیستم اولین شروعی را که
  «کل مدت سفارش» در آن جا می‌شود دقیق محاسبه و پیشنهاد می‌کند (مثلاً ۱۳:۳۵).
- پیشنهاد با یک دکمه «📅 رزرو در ساعت پیشنهادی» قابل ثبت است + دکمه
  «🔄 بررسی مجدد ظرفیت».
- پیش‌نمایش زندهٔ ظرفیت در صفحهٔ تأیید سفارش (🟢 موجود / 🔴 تکمیل) قبل از پرداخت.
- محاسبهٔ اوج مصرف به‌صورت event-based و دقیق (نه نمونه‌گیری زمانی)؛
  سفارش‌های «تکمیل و خروج» (بدون مدت) با تخمین امن لحاظ می‌شوند.
- حاشیهٔ ایمن پیش‌فرض ۱۰٪ از پول اکانت‌ها (`CAPACITY_SAFETY_BUFFER_PERCENT`)
  و سقف سفارش همزمان (`MAX_CONCURRENT_ORDERS`) هم اعمال می‌شود.
- **Fail-open:** هر خطای داخلی گارد، مسیر خرید را نمی‌بندد (فقط لاگ).
- ۲۱ تست آفلاین جدید (`tests/test_capacity_planner.py`).

### ۲) بازگردانی گزارش افزایش/کاهش موجودی ادمین‌ها در کانال گزارشات پرداختی

**مشکل:** شارژ/کسر موجودی کاربران توسط ادمین‌ها فقط به ادمین و کاربر اطلاع
داده می‌شد و هیچ ردپایی در «کانال گزارشات پرداختی» (`log_channel_payments`)
نمی‌ماند.

**راه‌حل (`services/payment_reporter.py`):**
- هر تغییر موجودی توسط ادمین، گزارش شکیل (مبلغ، کاربر، ادمین عامل، موجودی
  جدید، ربات، توضیح و زمان شمسی) به همان کانالی می‌رود که رسیدهای پرداخت
  آنلاین می‌روند — همهٔ جریان مالی در یک‌جا.
- ارسال با Markdown + fallback متن ساده؛ هرگز عملیات مالی ادمین را نمی‌شکند.
- تست‌های آفلاین (`tests/test_payment_reporter.py`).

### متفرقه
- `BOT_VERSION` به `2.2.2` بروزرسانی شد (برچسب قدیمیِ جا ماندهٔ 2.2.1).
- متغیرهای محیطی جدید در `.env.example` مستند شدند.

</div>

---

## نسخهٔ ۲.۲.۱ — 🚪 خروج مدیریت‌شده از ویس‌کال (ضد burst)

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۱

### مشکل
بعد از **پایان** یا **لغو** سفارش، همهٔ اکانت‌ها یک‌جا (هم‌زمان با `asyncio.gather`)
از ویس‌کال و گروه خارج می‌شدند. این burst شبیه رفتار ربات است و می‌تواند
FloodWait / محدودیت اکانت ایجاد کند.

### فیکس
- `VoiceCallManager.stop_all_for_order` و `cleanup_all` با **فاصلهٔ پلکانی**
  (`VOICE_LEAVE_STAGGER_MIN/MAX` پیش‌فرض ۰.۸–۱.۵ ثانیه) و **سقف هم‌زمانی**
  (`VOICE_LEAVE_MAX_CONCURRENCY=2`) اکانت‌ها را خارج می‌کنند.
- مانیتور سفارش **قبل** از leave متوقف می‌شود تا وسط خروج دوباره join نکند.
- `OrderExecutor._eject_all_fast` / `_cleanup_order` همان pacing را برای
  group/channel و leftoverها اعمال می‌کنند (بدون double-leave روی voice).
- ترتیب leave تصادفی می‌شود تا fingerprint ثابت نداشته باشد.
- کلیدهای env جدید در `.env.example` و `config.py`.

</div>

---

## نسخهٔ ۲.۲ — 🎨 UI رنگی + فیکس ایموجی پریمیوم

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۱ · **Bot API 9.4**

### 🐛 باگ‌فیکس‌های حیاتی

- **ایموجی پریمیوم بعد از یک خطا برای همیشه خاموش می‌شد:** خطای
  `Can't parse entities: unclosed end tag` (ناشی از برش وسط تگ `<tg-emoji>`
  در پیش‌نمایش بسته) کل چت ادمین را `mark_unsupported` می‌کرد و حتی با
  روشن‌بودن سوئیچ‌ها دیگر ایموجی پریمیوم ارسال نمی‌شد. حالا فقط خطاهای
  واقعیِ «custom emoji مجاز نیست» چت را blacklist می‌کنند؛ خطاهای parse
  با sanitize + fallback درمان می‌شوند و چت سالم می‌ماند.
- **پیش‌نمایش بسته:** پیام‌ها چندتکه (۲۵تایی) و با `safe_html_truncate`
  فرستاده می‌شوند تا تگ وسط برش نخورد و سقف ۱۰۰ entity رعایت شود.
- **کشف از اکانت (MTProto):** پارامتر درست `stickerset` (نه `sticker_set`)
  برای kurigram/Layer جدید — خطای `GetStickerSet unexpected keyword` رفع شد.
- **بازنشانی blacklist در استارت** و دکمهٔ «♻️ بازنشانی چت‌های مسدود» در پنل.

### ✨ قابلیت‌های جدید UI

- **دکمه‌های رنگی Bot API 9.4** روی همهٔ inline/reply:
  سبز (`success`) = تایید/پرداخت/فعال · قرمز (`danger`) = حذف/انصراف/رد ·
  آبی (`primary`) = بازگشت/ادامه/تنظیمات. تشخیص خودکار از متن دکمه
  (`classify_button_style`) + امکان تنظیم دستی.
- **HTML ایمن:** `safe_html_truncate` / `sanitize_html` / `strip_tg_emoji_tags`
  + `blockquote` / `expandable` / `divider` / `section_title` برای UI تمیز.
- **خوش‌آمدگویی HTML مینیمال** با موجودی فرمت‌شده و کیبورد پایدار.
- کلید env جدید: `PREMIUM_EMOJI_COLORED_BUTTONS=true`

</div>

---

## نسخهٔ ۲.۱ — 💎 ایموجی پریمیوم (Custom Emoji)

<div dir="rtl">

**تاریخ:** ۱۴۰۵/۰۶/۲۱ · **مبتنی بر Bot API 9.4 (۹ فوریهٔ ۲۰۲۶)**

از این نسخه، اگر اکانتِ **مالکِ ربات** اشتراک Telegram Premium داشته باشد،
کل خروجی‌های ربات (منوها، متن‌ها، دکمه‌ها، تیکت، بازنشر پیام کاربر) با
ایموجی پریمیوم/انیمیشنی ارسال می‌شود. راهنمای کامل:
[`docs/premium-emoji.fa.md`](docs/premium-emoji.fa.md)

### ✨ قابلیت‌های جدید

- **لایهٔ ایموجی پریمیوم در خروجیِ ربات:** کلاس‌های `PremiumEmojiBot`
  (زیرکلاسِ `ExtBot`) و `PremiumEmojiApplication` در `utils/premium_bot.py`
  همهٔ متدهای `send_message` / `edit_message_text` / `send_photo` /
  `send_video` / `send_document` / `send_animation` / `send_audio` /
  `send_voice` / `send_media_group` را ارتقا می‌دهند. چون در PTB همهٔ
  مسیرهای ارسال (مثل `context.bot`، `message.reply_text`،
  `callback_query.edit_message_text`، `send_safe`) در نهایت به همین متدها
  می‌رسند، **هیچ هندلری نیاز به تغییر نداشت** و همهٔ متن‌های موجود خودکار
  ارتقا می‌یابند.
- **ارتقای هوشمند متن در هر parse_mode:**
  - HTML → تبدیل ایموجی به `<tg-emoji emoji-id="…">` (بدون تگ تودرتو:
    داخل `<code>`/`<pre>`/`<tg-emoji>` موجود و داخل attributeها دست
    نمی‌خورد)
  - بدون parse_mode → افزودن entity از نوع `custom_emoji` با آفست‌های دقیقِ
    UTF-16 (برای متن فارسی هم درست است)
  - Markdown → تبدیل به HTML (`**بولد**` → `<b>بولد</b>`) و سپس ارتقا
  - سقف ۹۰ ایموجی در هر پیام (تلگرام حداکثر ۱۰۰ entity می‌پذیرد)
- **آیکون پریمیوم روی دکمه‌ها:** ایموجی اول/آخرِ متنِ دکمه‌های **inline** و
  **کیبورد اصلی (reply)** به فیلد رسمی `icon_custom_emoji_id` تبدیل و از
  متن حذف می‌شود (آیکون قبل از متن رندر می‌شود؛ بدون این کار دو ایموجی
  دیده می‌شد).
- **لایهٔ بازگردانی برچسب دکمه‌ها:** هر برچسبی که ایموجی‌اش حذف شده در یک
  نگاشت ثبت می‌شود و `PremiumEmojiApplication.process_update` پیش از
  dispatch هر آپدیت، متنِ دریافتی را عیناً به شکل اصلی برمی‌گرداند و آفست
  entityها را هم اصلاح می‌کند. نتیجه: ده‌ها فیلتر دقیق مثل
  `filters.Regex("^🆘 پشتیبانی$")` و همهٔ `ConversationHandler`ها **بدون
  هیچ تغییری** درست کار می‌کنند.
- **بازنشر ایموجی پریمیومِ خودِ کاربر:** در تیکت (پیام کاربر → ادمین و
  پاسخ ادمین → کاربر، شامل کپشن عکس/ویدئو/ویس/فایل) با `text_html` همان
  ایموجی سفارشیِ کاربر منتقل می‌شود. پیام همگانی و پیام خصوصیِ ادمین با
  `copy_message` / `message.copy()` به‌صورت ذاتی همهٔ entityها را حفظ
  می‌کنند.
- **بستهٔ آمادهٔ شناسه‌ها:** ۱۶۳ کلید معنایی (`check`, `cross`, `rocket`,
  `wallet`, `support`, `settings`, `voice`, `clock`, …) + نگاشتِ
  «یونیکد → شناسه» برای **۳۸۱** ایموجی (با و بدون Variation Selector) به
  انضمام صدها معادلِ دستی؛ پوششِ کاملِ ۱۵۳ ایموجی یونیکدِ موجود در متن‌های
  ربات. (`utils/premium_emoji.py`)
- **اعتبارسنجی خودکار در هر استارت:** همهٔ شناسه‌ها با متد رسمی
  `getCustomEmojiStickers` سنجیده می‌شوند (هر ۲۰۰ شناسه یک فراخوانی)؛
  شناسهٔ نامعتبر از مدار خارج شده و ایموجی یونیکد جای آن را می‌گیرد.
- **پنل ادمینِ ایموجی پریمیوم:** `⚙️ تنظیمات سیستم → 💎 ایموجی پریمیوم`
  شامل چهار کلید روشن/خاموش (ذخیره در دیتابیس)، «🧪 پیام تست»، «📋
  پیش‌نمایش بسته»، «🔄 اعتبارسنجی شناسه‌ها»، «🔧 جایگزینی شناسه» و «🛜
  کشف از اکانت (MTProto)» برای خواندن پک‌های ایموجی دلخواه.
  (`handlers/premium_emoji_handlers.py`)
- **سرویس پایدارِ تنظیمات:** کلیدهای تنظیم در دیتابیس ذخیره می‌شوند و بر
  `.env` اولویت دارند. (`services/premium_emoji_service.py`)
- **ابزار خط فرمان:** `python tools/premium_emoji_sync.py --list |
  --validate | --discover PACKS` برای اعتبارسنجی و کشف شناسه‌ها بیرون از
  ربات، با خروجی آماده برای `.env`.
- **fallback خودکار و تفکیک چندرباتیه:** اگر تلگرام payload ارتقایافته را
  نپذیرد (کانال، مالک بدون پریمیوم، شناسهٔ نامعتبر)، بلافاصله همان پیام با
  payload اصلی ارسال می‌شود و آن چت — به‌تفکیک توکن هر ربات — از فهرست
  ارتقا خارج می‌شود تا دوباره تلاش نشود. ربات‌های نمایندگی هم همین لایه را
  دارند. (`services/bot_manager.py`)

### 🐛 رفع اشکال‌های مهم

- **رفع شکستن HTML در تیکت:** متن پیام کاربر، موضوع تیکت و نام‌ها که از
  دیتابیس می‌آمدند بدون escape داخل پیام HTML قرار می‌گرفتند و هر متن
  حاوی `<` یا `&` باعث `Can't parse entities` و از دست رفتن اعلان می‌شد.
  اکنون همهٔ محتوای دیتابیس با `escape_outside_tags` امن می‌شود و تنها
  تگ‌های ساختهٔ خودِ ربات سالم می‌مانند.
- **رفع بریدن وسط تگ در پیش‌نمایش:** بریدن متنِ HTML‌شده در کاراکترِ ثابت
  می‌توانست یک `<tg-emoji>` را نصف کند؛ اکنون برای پیام‌های کوتاه (≤۶۰
  کاراکتر) خودِ ایموجی پریمیومِ کاربر نمایش داده می‌شود و برای پیام‌های
  بلند، پیش‌نمایشِ متنیِ escape‌شده.
- **رفع گم‌شدن ایموجی پریمیوم در پاسخ ادمین:** پاسخ ادمین به کاربر حالا با
  `parse_mode="HTML"` و `message_content_html` فرستاده می‌شود تا ایموجی
  سفارشیِ ادمین (در متن و کپشن عکس/ویدئو/ویس/فایل) به کاربر برسد.

### ⚙️ تغییرات فنی

- **تغییر مسیرِ parse_mode:** پیام‌هایی که قبلاً با `parse_mode=MARKDOWN`
  (پیش‌فرض `send_safe`) ارسال می‌شدند، حالا به HTML تبدیل می‌شوند؛ این
  تبدیل دقیقاً با قواعد Markdown قدیمیِ تلگرام هم‌خوان است
  (`*بولد*`/`**بولد**` → `<b>`، `_ایتالیک_` → `<i>`، `` `code` `` →
  `<code>`، ```` ```pre``` ```` → `<pre>`، `[متن](لینک)` → `<a href>`) و با
  `PREMIUM_EMOJI_MARKDOWN_TO_HTML=false` قابل خاموش کردن است.
- **نسخهٔ ربات:** ثابت `BOT_VERSION = "2.1.0"` در `constants.py` اضافه شد و
  در لاگ استارت و پنل ایموجی پریمیوم نمایش داده می‌شود.
- **کلیدهای `.env` جدید:** `PREMIUM_EMOJI_ENABLED`, `PREMIUM_EMOJI_TEXT`,
  `PREMIUM_EMOJI_BUTTONS`, `PREMIUM_EMOJI_REPLY_BUTTONS`,
  `PREMIUM_EMOJI_TRAILING_ICONS`, `PREMIUM_EMOJI_MARKDOWN_TO_HTML`,
  `PREMIUM_EMOJI_VALIDATE`, `PREMIUM_EMOJI_SKIP_CHANNELS`,
  `PREMIUM_EMOJI_MAX_PER_MESSAGE`, `PREMIUM_EMOJI_OVERRIDES`.
- **۶۱ تست آفلاین جدید** در `tests/test_premium_emoji.py` (بدون هیچ تماس
  شبکه‌ای): بستهٔ شناسه‌ها، آفست UTF-16، ارتقای HTML، تبدیل Markdown،
  ارتقای markup، بازگردانی برچسب، بازنشر محتوای کاربر، کل خط لولهٔ بات
  شامل fallback و سقف entity، و مسیر اعتبارسنجی/کشف نامربوطی ایموجی.
- **کنترلِ «ایموجی نامربوط»:** `getCustomEmojiStickers` فیلد `Sticker.emoji`
  را هم برمی‌گرداند؛ اگر ایموجیِ واقعیِ یک شناسه با ایموجیِ انتظارِ بسته
  نخواند، در لاگ و پنل ادمین گزارش می‌شود و با
  `PREMIUM_EMOJI_STRICT_MATCH=true` خودکار غیرفعال هم می‌شود.
- **پوشش کامل‌تر نگاشت یونیکد:** با اسکن خودکارِ کل مخزن، ۳۰ معادلِ دیگر
  (🔸 🔹 🔻 ⬜️ 🔟 🏁 ♾ 📆 👮‍♂️ 🏳️ 📁 🚦 📜 ⏭ ⚰️ 🔋 🎉 🔘) به نگاشت اضافه شد تا
  همهٔ ایموجی‌های یونیکدِ متن‌های ربات (به‌جز پیکان‌های تایپی، پرچم و
  ری‌اکشن‌های داخل ویس‌کال) ارتقا یابند.

### 🔎 محدودیت‌های تلگرام (طبیعی، نه باگ)

- ایموجی سفارشی در **کانال** مجاز نیست (فقط خصوصی/گروه/سوپرگروه) → کانال‌ها
  خودکار رد می‌شوند.
- `answerCallbackQuery` (toast دکمه‌ها) و `setMyCommands` از entity/HTML
  پشتیبانی نمی‌کنند → ایموجی یونیکد می‌مانند.
- پیش‌نیاز: اشتراک **Telegram Premium** روی اکانتِ سازندهٔ ربات در BotFather.

</div>

---

## نسخهٔ ۲.۰ — چت داخل تماس + پایداری لغو سفارش

<div dir="rtl">

### ✨ قابلیت‌های جدید

- **مرکز چت داخل تماس (In-Call Chat):** مشتری می‌تواند از اکانت‌های فعالِ یک
  سفارش، به‌صورت تک یا گروهی (multi-select)، کامنت یا ری‌اکشن بفرستد. منو
  «قابل استفادهٔ مکرر» است: تا وقتی سفارش فعال است می‌توان بارها پیام/ری‌اکشن
  فرستاد. (`handlers/incall_handlers.py`)
- **ارسال واقعی پیام/ری‌اکشن در ویس‌کال:** با ارتقا به **kurigram** (fork
  نگهداری‌شدهٔ Pyrogram با Layer جدید تلگرام) متد
  `phone.sendGroupCallMessage` واقعاً کار می‌کند.
- **کلید روشن/خاموش برای ادمین:** قابلیت چت داخل تماس از پنل ادمین
  فعال/غیرفعال می‌شود (کلید تنظیم: `service_incall_chat`).
- **انتخاب سفارش هوشمند:** اگر مشتری چند سفارش فعال داشته باشد، ابتدا سفارش
  موردنظر را انتخاب می‌کند.

### 🐛 رفع اشکال‌های مهم

- **رفع خرابیِ کاملِ ورود اکانت‌ها به ویس‌کال:** پس از سوییچ به kurigram،
  یک monkeypatch قدیمی به ثابتِ حذف‌شدهٔ `pyrogram.utils.MIN_CHAT_ID` اشاره
  می‌کرد و باعث می‌شد **هیچ اکانتی** نتواند وارد تماس شود. اکنون این patch
  فقط روی Pyrogram کلاسیک اجرا می‌شود و روی kurigram (که خودش شناسه‌های
  مدرن کانال را پشتیبانی می‌کند) رد می‌شود. (`services/voice_call_manager.py`)
- **رفع burst در ارسال پیام/ری‌اکشن گروهی:** پیش‌تر همهٔ ارسال‌ها در یک لحظه
  شلیک می‌شد (`asyncio.gather`) و باعث FloodWait و دیده‌نشدن پیام می‌شد.
  اکنون ارسال‌ها با فاصلهٔ منظم (~۱ درخواست در ثانیه) و سقف هم‌زمانی محدود
  انجام می‌شوند: نه یک‌دفعه‌ای، نه کند. قابل تنظیم با
  `INCALL_SEND_STAGGER_MIN/MAX` و `INCALL_SEND_MAX_CONCURRENCY`.
- **رفع گزارش لغوِ تکراری/ناقص:** هنگام لغو سفارش، دو گزارش به کانال می‌رفت
  (یکی کامل، یکی ناقص با «—»). اکنون فقط **یک گزارش کامل و درست** ارسال
  می‌شود. (flag یک‌بارمصرفِ سرکوب گزارش در `services/order_executor.py`)
- **رفع `CHANNEL_INVALID` در ارسال چنداکانتی:** `access_hash` کانال برای هر
  اکانت متفاوت است؛ اکنون peer با سشن همان اکانتِ فرستنده resolve می‌شود.
- **رفع `GROUPCALL_INVALID`:** رفرش خودکارِ رفرنسِ کهنهٔ تماس هنگام ارسال.
- **رفع اسپمِ healthcheck پستگرس:** افزودن `-d` به `pg_isready`.

### ⚙️ تغییرات فنی

- **کتابخانهٔ MTProto:** جایگزینی Pyrogram با `kurigram>=2.2.25` (با نام
  `pyrogram` ایمپورت می‌شود؛ drop-in). سشن‌استرینگ‌های قبلی معتبر می‌مانند.
  `py-tgcalls==2.2.5` بدون تغییر کار می‌کند.
- **دکمه‌های بازگشت:** شبکهٔ ایمنیِ سراسری برای دکمهٔ «🔙 بازگشت» تا هیچ دکمه‌ای
  بی‌اثر نماند.

</div>

---

## نسخه‌های پیشین (خلاصه)

<div dir="rtl">

- **پنل اکانت‌ها (glass UI):** بازطراحی فهرست اکانت‌ها و منوهای ویرایش با
  دکمه‌های شیشه‌ای inline، بدون «آیدی را تایپ کنید».
- **پرداخت چنددرگاهی:** پشتیبانی هم‌زمان از **زرین‌پال** و **آقای پرداخت** +
  منوی انتخاب درگاه؛ عبور درخواست‌های درگاه از پروکسیِ بدون WARP.
- **تیکتینگ حرفه‌ای:** بازطراحی کامل سیستم تیکت + بستن خودکار.
- **گزارش‌های مالی دقیق:** تسویهٔ ثانیه‌ای (pro-rated) و انتخاب عودت/عدم‌عودت
  برای ادمین.
- **زیرساخت WARP ایزوله:** کل ترافیک ربات از کانتینر WARP عبور می‌کند تا
  مدیای صوتی (UDP) پایدار بماند، بدون لمس SSH/شبکهٔ هاست.
- **بهینه‌سازی CPU:** ffmpeg تک‌ترد، صدای سکوت مونو ۲۴kHz، لاگ‌های کم‌حجم،
  uvloop.
- **Adaptive Join Brain:** مدیریت هوشمند هم‌زمانی و backoff برای کاهش
  FloodWait هنگام ورود اکانت‌ها.

</div>

---

<div dir="rtl">

راهنمای کامل راه‌اندازی روی سرور جدید:
[`docs/new-server-setup.fa.md`](docs/new-server-setup.fa.md)

</div>
