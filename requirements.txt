#Core

numpy
python-dotenv
cryptography
# ⚠️ حتماً با extra «asyncio»: این پروژه sqlalchemy.ext.asyncio را import
# می‌کند و از SQLAlchemy 2.1 به بعد، greenlet دیگر خودکار نصب نمی‌شود.
# با «sqlalchemy» خالی، بیلدِ تازه روی سرور جدید در همان import اولِ
# database.py با این خطا کرش می‌کند:
#   ImportError: The SQLAlchemy asyncio module requires that the Python
#   'greenlet' library is installed. ... use 'sqlalchemy[asyncio]'
# (به‌صورت عملی روی SQLAlchemy 2.1.1 بازتولید شد.)
sqlalchemy[asyncio]>=2.0,<2.2
greenlet>=3.0
asyncpg
redis
aiohttp
jdatetime
# Faster asyncio event loop (Linux/macOS only; skipped on Windows).
# Cuts event-loop overhead which lowers CPU under many concurrent voice tasks.
uvloop; sys_platform != "win32"

#Bot

# پینِ محدودهٔ نسخه: کد روی API نسخهٔ ۲۲ نوشته شده (Application/Persistence،
# ایموجی پریمیوم Bot API 9.4). نصبِ بدون پین می‌تواند نسخهٔ ۲۳ را بیاورد و
# استارت را بشکند.
python-telegram-bot[job-queue]>=22.0,<23

#Engine

# Kurigram: fork نگهداری‌شدهٔ Pyrogram که با نام «pyrogram» ایمپورت می‌شود
# (drop-in replacement). این نسخه Layer جدید تلگرام (>=216) و در نتیجه
# متد phone.sendGroupCallMessage (پیام/ری‌اکشن درون ویس‌کال) را پشتیبانی می‌کند.
# چون هم‌نام pyrogram است، py-tgcalls و بقیهٔ کد بدون تغییر کار می‌کنند و
# سشن‌استرینگ‌های قبلی نیز معتبر می‌مانند.
# نکته: pyrogram اصلی نباید هم‌زمان نصب باشد (تداخل پکیج). Dockerfile قبل از
# نصب، pyrogram/pyrofork را حذف می‌کند.
# پین دقیق نسخه برای بیلدهای تکرارپذیر (join_chat در 2.2.26 از Chat به
# ChatJoinResult تغییر کرد؛ کد هر دو حالت را پشتیبانی می‌کند).
kurigram==2.2.26
tgcrypto>=1.2.5
# py-tgcalls 2.3.x لازم است: نسخه‌های 2.2.x روی لایه‌های جدید تلگرام
# (kurigram) با UpdateGroupCall بدون chat_id کرش می‌کنند
# (AttributeError در on_update) و لاگ را اسپم می‌کنند.
py-tgcalls==2.3.3
